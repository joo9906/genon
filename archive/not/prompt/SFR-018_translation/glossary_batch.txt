{#
  용어사전 지시 — 번역 batch 경로. **용어가 있을 때만** 조립된다
  (`prompt_builder.py`). 비어 있으면 이 파일을 렌더하지 않고 빈 문자열을 넘긴다 —
  등장하지 않는 용어까지 지시하면 모델이 억지로 끼워 넣는다.

  변수
    entries : '- "원문" -> "번역"' 줄들을 개행으로 이은 것 (**꼬리 개행 없음**).

  이 파일은 `system_batch.txt` 의 `{{ glossary_block }}` 자리에 그대로 들어간다.
  맨 앞 빈 줄이 문체 절과의 경계다 — 지우면 두 절이 붙는다.
#}

[용어사전 — 반드시 따른다]
원문 용어가 나오면 아래에 적힌 대상 용어를 **그대로** 씁니다. 문장에 맞게 활용형은 바꾸되
동의어로 대체하지 않습니다. 목록에 없는 용어는 자유롭게 번역합니다.
{{ entries }}