# SFR-018 글다듬이 — 코드 서빙(03) 빌드 의존
#
# 이 파일은 **이 단위가 03 이 되면서 처음 생겼다.** 이전에는 워크플로우(02) 단위라
# 의존성 파일이 필요 없었다 — 02 는 pod 기본 이미지에 있는 패키지만 쓸 수 있고
# `requirements.txt` 를 설치하지 않는다 (가이드 11.5.6).
#
# 가이드 6.3: 빌드 (Build) 커맨드에서 `pip install -r requirements.txt` 를 실행한다.
# 가이드 11.5.6: 폐쇄망이면 사내 PyPI registry/mirror 접근 여부를 먼저 확인한다.

fastapi>=0.110
uvicorn>=0.30
pydantic>=2.6
# Gateway 경유 LLM 호출 (text_polish/llm.py)
httpx>=0.27
# ── 없는 것: `jinja2` (2026-09-07 제거) ──────────────────
# 프롬프트 렌더가 `{{ 이름 }}` 치환만 하게 바뀌면서 jinja2 를 안 쓴다 (`prompt_loader.py`
# 머리말). **선언만 남아 있으면 사내 mirror 에 그 패키지가 없을 때 `pip install -r` 이
# 그 자리에서 실패해 배포가 통째로 막힌다** — 코드가 쓰지도 않는 이유로.

# ── 없는 것 ────────────────────────────────────────────────
# **`openai` 를 2026-09-07 에 걷어냈다.** 실환경에서 SDK 때문에 호출이 실패했고,
# 게이트웨이가 OpenAI 호환 경로를 내주므로 `httpx` 로 직접 POST 하면 된다 —
# FAQ·006 두 단위는 처음부터 그 모양이었다. 근거는 `text_polish/llm.py` 머리말.
#
# lxml·redis 를 쓰지 않는다. 이 단위는 마크다운 텍스트만 다루고 상태를 갖지 않는다.
#
# 구조·사실 점검(markdown_guard·fact_guard·diff_report)은 `genon_text_guard` MCP 서빙으로
# 옮겼다. 그 셋은 표준 라이브러리만 쓰므로 어느 쪽에 있어도 의존이 늘지 않지만,
# MCP 에 두면 번역·FAQ 를 포함한 **모든 워크플로우가 같은 판정을 쓴다.**

# ── 이 판본만의 의존: `openai` SDK ────────────────────────────
# 정본(`onprem/`)은 2026-09-07 에 이 패키지를 걷어내고 `httpx` 로 직접 부른다. 여기는
# 그 반대 판본이다 — 어느 쪽이 실환경 게이트웨이에서 뜨는지 확인하려고 나란히 둔 것이고,
# **둘 중 하나만 등록한다.** 사내 mirror 에 이 패키지가 없으면 `pip install -r` 이 그
# 자리에서 실패하므로, 등록 전에 mirror 를 먼저 확인할 것 (가이드 11.5.6).
# `httpx` 는 SDK 가 내부에서 쓰고 우리 코드(file_store 등)도 쓰므로 위에 그대로 있다.
openai>=1.30
