{#
  값 추출 유저 프롬프트의 **본문 구획** — `extract_user.txt` 의 `{{ body_section }}` 자리에
  그대로 들어간다 (2026-09-07 신규).

  **파일로 뗀 이유**: 이 구획은 "쓸 수 있는 서식이 하나라도 있을 때만" 실린다. 조건을
  템플릿에서 걷어내면서 문구를 `prompts.py` 로 옮길 수도 있었지만, 그러면 프롬프트 문장이
  코드에 살게 된다(가이드 §10.5 가 금지하는 모양이고, 프롬프트 라이브러리로 덮을 수도
  없어진다). 조건은 코드가, 문구는 파일이 갖는다.

  변수
    style_lines : "- 항목명" 줄들을 개행으로 이은 것.
    block_lines : "번호. [서식] 내용" 줄들을 개행으로 이은 것. **하나도 없으면 `(없음)`** —
      그 판단은 `prompts.py` 가 한다(빈 자리를 남기면 모델이 무엇을 뜻하는지 모른다).

  **꼬리 개행이 계약이다.** 이 파일의 렌더 결과는 개행으로 끝나야 `extract_user.txt` 의
  `[사용자 발화]` 앞에 빈 줄이 하나 남는다 — 서식 목록이 없을 때(빈 문자열)와 같은 모양이다.
#}
[본문 서식 목록]
{{ style_lines }}

[현재 본문 추가 내용]
{{ block_lines }}
