# GenOS 폐쇄망 문서 자동화 — 설계서

| 항목 | 내용 |
|---|---|
| 대상 시스템 | 사내 GenOS 플랫폼 문서 자동화 기능 4종 |
| 저장소 | joo9906/genon |
| 현행 구현 경로 | `onprem/` (유일한 구현) |
| 등록 단위 | 9개 = 코드서빙 4 + MCP 도구 파일 4 + hwpx 전처리기 1 |
| 워크플로우 스텝 | 9개 (캔버스 등록, 서빙 아님) |
| 작성 기준일 | 2026-08-26 (코드 기준 2026-08-18 검증본) |

---

## 0. 설계 원칙

이 시스템 전체를 관통하는 원칙은 하나다.

**판정은 LLM에 맡기지 않는다.** LLM은 값 추출과 문장 생성만 한다. "채워졌는가 / 구조가 깨졌는가 / 근거가 실제로 있는가"는 코드가 결정적으로 판정한다.

| 파생 규칙 | 이유 |
|---|---|
| MCP 도구는 LLM을 부르지 않는다 | 같은 입력에 항상 같은 결과가 나와야 검증이 성립한다 |
| 워크플로우 스텝은 httpx만 쓴다 | 기본 이미지 패키지 변경 요청에 배포가 묶이지 않게 |
| 배포 단위 간 import 금지 | GenOS 등록 단위가 서로 독립 컨테이너/파일이다 |
| 오류는 예외가 아니라 data["error"] | 예외를 던지면 워크플로우가 통째로 죽어 안내문이 사용자에게 못 간다 |
| 침묵 처리 금지 | 기각·강제·미판정은 전부 응답에 드러낸다 |

---

## 1. 서비스 워크플로우

### 1-1. 영역 구성

| 구분 | area 01 MCP | area 02 워크플로우 | area 03 코드서빙 | area 05 전처리기 |
|---|---|---|---|---|
| 등록 단위 | 파일 1개 = 도구 묶음 (4) | 파일 1개 = 스텝 1개 (9) | 디렉토리 = 서빙 (4) | 파일 1개 (1) |
| 진입점 | `@mcp.tool()` | `run(data)` | FastAPI 앱 + `$PORT` | `DocumentProcessor.__call__` |
| 외부 패키지 | stdlib (hwpx만 lxml) | httpx 뿐 | fastapi·lxml·redis·jinja2·openai | stdlib + lxml |
| LLM 호출 | 없음 | 없음 | 있음 | 없음 |
| 상태 저장 | 없음 | 없음 | Redis (006/FAQ) | 없음 |
| 비고 | 앱·포트·health 없음 | 자기완결 필수 | 컨테이너 1개 = URL 1개 | 위 흐름에 배선되지 않음 |

### 1-2. 기능별 영역 배치

| 기능 | 워크플로우 스텝 | 코드서빙 | 사용 MCP | 산출물 |
|---|---|---|---|---|
| SFR-006 HWPX 템플릿 채우기 | 3 | SFR-006_template_fill | 없음 | hwpx |
| SFR-018 글다듬이 | 2 | SFR-018_text_polish | lang_policy, text_guard | txt |
| SFR-018 번역 | 2 | SFR-018_translation | lang_policy, text_guard, hwpx_text, glossary | txt |
| SFR-018 FAQ 생성 | 2 | SFR-018_faq | hwpx_text | txt |

018 세 기능의 산출물은 txt 하나로 통일되어 있다(2026-08-12 요구 변경). 사용자가 결과를 메모장에서 이어 편집하기 때문이다. 파일은 UTF-8 BOM + CRLF로 낸다 — 옛 윈도우 메모장이 BOM 없는 UTF-8을 cp949로 읽어 한글을 깨뜨리고, LF만 있는 파일을 한 줄로 붙여 보여준다. 006만 hwpx로 내며, 사내 양식을 채우는 것이 기능 자체이기 때문이다.

### 1-3. SFR-006 템플릿 채우기 워크플로우

| 순서 | 주체 | 하는 일 | 호출 대상 |
|---|---|---|---|
| 1 | 관리자 | hwpx 양식 등록 + 즉시 색인 (파싱 먼저, 파일 쓰기 나중) | POST /templates |
| 2 | 사용자 | 캔버스에서 템플릿 선택 | 변수 template_fill_template_id |
| 3 | 스텝 1 | 항목 목록·기존 값 조회, ready_for_download 산출 | POST /chat/context |
| 4 | 캔버스 | ready_for_download로 분기 (완료 시 다운로드 안내 노드로) | — |
| 5 | 스텝 2 | LLM이 발화에서 값 추출, 코드가 화이트리스트 검증 | POST /chat/extract |
| 6 | 스텝 3 | 세션 병합·저장·미리보기, 토큰 스트리밍 후 result 1회 | POST /chat/commit |
| 7 | 사용자 | 다운로드 버튼 → 초안 hwpx 수신 | POST /generate |

채울 자리 인식 방식은 우선순위대로 슬롯 → 누름틀 → {{token}} 세 가지다.

| 방식 | 생김새 | 상태 |
|---|---|---|
| 슬롯 | `제 목 : {'제목', 16pt, 맑은 고딕, 볼드}` | 기본 (현장 템플릿이 이 모양) |
| 누름틀 (CLICK_HERE) | 한/글에서 심은 필드 | 폴백 |
| {{token}} | `부서: {{dept}}` | 프로토타입 호환용 |

문서 조립 순서가 계약이다: 서식 적용 → 채우기 → 본문 블록. `document.build` 한 곳에만 있다. 채우면 중괄호 자체가 사라져 어디에 무슨 서식을 걸지 알 수 없기 때문에 서식이 먼저다.

### 1-4. SFR-018 글다듬이 워크플로우

| 순서 | 주체 | 하는 일 | 호출 대상 |
|---|---|---|---|
| 1 | 스텝 1 | 업로드 문서 또는 채팅 텍스트로 원본 확보 | 없음 |
| 2 | 스텝 1 | 문서유형 → 톤 확정, 강제 여부 판정 | MCP resolve_tone |
| 3 | 스텝 2 | 문서를 통째로 LLM에 보내 다듬기 | POST /polish |
| 4 | 스텝 2 | 구조 지문 대조 | MCP markdown_structure_issues |
| 5 | 스텝 2 | 숫자·날짜 보존 대조 | MCP fact_issues |
| 6 | 스텝 2 | 문장 단위 변경 내역 산출 | MCP diff_changes |
| 7 | 사용자 | txt 내려받기 | POST /download |

번역과 달리 문서를 통째로 LLM에 보낸다(문장 문맥이 필요하다). 대신 전/후 구조 지문을 대조한다. 훼손을 감지해도 되돌리지 않고 경고만 낸다. 톤·문서유형은 고객사 관리자가 GenOS 프롬프트 라이브러리에 JSON을 등록해 재배포 없이 추가할 수 있다.

### 1-5. SFR-018 번역 워크플로우

| 순서 | 주체 | 하는 일 | 호출 대상 |
|---|---|---|---|
| 1 | 스텝 1 | hwpx 경로가 있으면 직접 파싱 (전처리기 우회) | MCP hwpx_to_markdown |
| 2 | 스텝 1 | 번역 방향이 지원 범위인지 검증 | MCP validate_direction |
| 3 | 스텝 2 | 번역 요청 | POST /translate/markdown |
| 4 | 코드서빙 | 스켈레톤 분해 → LLM(셀/문장만) → 재조립 | 내부 |
| 5 | 코드서빙 | 용어사전 준수율 재계산 | 내부 glossary_report |
| 6 | 스텝 2 | 숫자 보존 확인 | MCP numeric_issues |
| 7 | 사용자 | txt 내려받기 | POST /download |

지원 범위는 6개 언어(한국어·영어·중국어·태국어·베트남어·러시아어)이고, 원본이나 대상 중 하나는 반드시 한국어여야 한다. en→ru는 400이다. 구조 보존 계약은 둘이다. 첫째 무손실 — 항등 번역이면 산출물이 입력과 문자 단위로 같다. 둘째 구조 불변 — 번역 후에도 줄마다 파이프 수·마커가 원본과 같다.

### 1-6. SFR-018 FAQ 생성 워크플로우

| 순서 | 주체 | 하는 일 | 호출 대상 |
|---|---|---|---|
| 1 | 스텝 1 | hwpx 직접 파싱(우선) 또는 전처리기 산출물 확보 | MCP hwpx_to_markdown |
| 2 | 스텝 1 | 배포 상한 확인 후 개수 결정 (배포 ∩ 캔버스 ∩ 요청) | GET /config |
| 3 | 스텝 2 | LLM이 Q·A·근거 생성 | POST /generate |
| 4 | 코드서빙 | 근거가 실제로 문서에 있는지 대조, 없으면 기각 | 내부 evidence.py |
| 5 | 코드서빙 | 기각 건수 전부 노출 (schema/ungrounded/duplicate) | 내부 |
| 6 | 사용자 | txt 내려받기 (저장된 것을 내려준다, 재생성하지 않는다) | POST /download |

---

## 2. 흐름도

### 2-1. 전체 아키텍처

```
                        ┌─────────────────┐
                        │  사용자 / 캔버스 UI  │
                        └────────┬────────┘
                                 │
        ┌────────────────────────▼────────────────────────┐
        │        area 02  워크플로우 스텝 9개 (httpx만)         │
        │                                                 │
        │  sfr006_01_context   sfr018_polish_01_policy     │
        │  sfr006_02_extract   sfr018_polish_02_polish     │
        │  sfr006_03_commit    sfr018_translate_01_detect  │
        │  sfr018_faq_01_source sfr018_translate_02_...    │
        │  sfr018_faq_02_generate                          │
        └────────────────────────┬────────────────────────┘
                                 │
                        ┌────────▼────────┐
                        │  GenOS 게이트웨이   │
                        └────┬───────┬────┘
                             │       │
        ┌────────────────────▼──┐ ┌──▼──────────────────────┐
        │ area 03 코드서빙 4단위   │ │ area 01 MCP 도구 파일 4개 │
        │                       │ │      (LLM 없음)          │
        │ SFR-006_template_fill │ │ genon_lang_policy       │
        │ SFR-018_text_polish   │ │ genon_text_guard        │
        │ SFR-018_translation   │ │ genon_hwpx_text         │
        │ SFR-018_faq           │ │ genon_glossary          │
        └───┬───────────────┬───┘ └─────────────────────────┘
            │               │
    ┌───────▼──────┐  ┌─────▼──────┐
    │  LLM 게이트웨이  │  │ Redis 세션  │
    └──────────────┘  └────────────┘

    [별도 경로] area 05 hwpx 전처리기 — RAG 적재용, 위 흐름에 배선되지 않음
```

### 2-2. SFR-006 대화 한 턴

```
사용자 발화 + template_id
      │
      ▼
[스텝1 context] ──POST /chat/context──▶ [코드서빙 006] ──▶ [Redis 세션 조회]
      │                                       │
      │◀── field_names, field_values, fields_missing ──┘
      │
      ├─ ready_for_download 산출 (캔버스 분기 근거)
      │
      ├─ 발화가 비어 있음 ──────────────────────┐
      │   (LLM 호출 안 함 — 빈 발화로 부르면      │
      │    항목을 지어낸다)                      │
      │                                        │
      ▼ 발화 있음                                │
[스텝2 extract] ──POST /chat/extract──▶ [코드서빙] ──▶ [LLM]
      │                                       │
      │                          ┌────────────┘
      │                          ▼
      │                 화이트리스트 검증 · 값 보존 확인 (코드)
      │                          │
      │◀── fields_updated / fields_rejected ──┘
      │                                        │
      ▼◀───────────────────────────────────────┘
[스텝3 commit] ──POST /chat/commit──▶ [코드서빙] ──▶ [Redis 병합 저장]
      │                                    (값+블록 함께 — 저장은 덮어쓰기)
      │◀── document_markdown, fields_missing ──┘
      │
      ▼
token 스트리밍 (32자 단위) → event: result 정확히 1회
      │
      ▼
사용자 다운로드 버튼 ──POST /generate──▶ hwpx 파일
```

### 2-3. 번역 — 스켈레톤 분리·재조립

```
[hwpx / 마크다운 원본]
        │
        ▼
   hwpx 경로 있나?
    ├─ 있음 ──▶ [MCP hwpx_to_markdown] 직접 파싱
    └─ 없음 ──▶ [전처리기 산출 마크다운]
        │
        ▼
   [MCP validate_direction]
    ├─ allowed=false ──▶ 고정 안내문 (재시도 무의미, retryable=false)
    └─ allowed=true
        │
        ▼
   [스켈레톤 분해]
    표 파이프 · HTML 태그 · 제목 · 목록 · 코드펜스를 코드가 분리
        │
        ▼
   [LLM 호출] 셀/문장 텍스트만 전달 · 같은 원문은 한 번만
        │
        ▼
   [재조립] 구조는 LLM 출력과 무관하게 원본과 동일
        │
        ▼
   [용어사전 준수율 재계산] glossary_report.build_report
        │
        ▼
   [MCP numeric_issues] 숫자 보존 확인
        │
        ▼
   [txt 다운로드] UTF-8 BOM + CRLF
```

### 2-4. FAQ 근거 검증 게이트

```
[LLM 응답: Q · A · evidence]
        │
        ▼
   스키마 유효한가? ──── 아니오 ──▶ [기각: schema]
        │ 예
        ▼
   evidence가 원문에 완전 포함? ─── 예 ──▶ [채택 score 1.0]
        │ 아니오                              │
        ▼                                    │
   3-gram 겹침 ≥ FAQ_EVIDENCE_MIN_RATIO?      │
        ├─ 예 ────────────────────────────────┤
        └─ 아니오 ──▶ [기각: ungrounded]        │
                                             ▼
                                      중복 문항인가?
                                       ├─ 예 ──▶ [기각: duplicate]
                                       └─ 아니오 ──▶ [최종 채택]

   ※ 기각 3종은 전부 stats로 응답에 노출한다.
     조용히 버리면 5개 요청에 3개만 나온 이유를 알 수 없다.
```

### 2-5. 오류 전파 규약

```
[중간 스텝 실패]
        │
        ▼
data["error"] = { error_code, retryable, msg }      ※ error_type은 싣지 않는다
        │                                             (내부 분류값 — 로그에만)
        ▼
[다음 스텝] data.error 있으면 아무것도 하지 않고 통과
        │
        ▼
[마지막 스텝] 오류 문구 token 스트리밍 → event: result 1회
        │
        ▼
   사용자 화면

   ※ 예외를 던지지 않는 이유: 워크플로우가 통째로 죽어 안내문이 못 간다.
   ※ 영역코드는 02- 다. 코드서빙(03-)의 코드를 그대로 올리지 않는다.
   ※ 재시도 판정은 상태코드가 아니라 응답 본문 error_code 분류(00020003)로 본다.
```

---

## 3. Tool / Function 목록

### 3-1. MCP 도구 — genon_lang_policy.py

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| detect_language | 문자 체계로 언어를 감지한다. LLM을 쓰지 않아 항상 같은 답이 나온다 | sample: str — 감지할 표본 텍스트 | JSON 문자열 {ok, lang, detected}. 감지 불가는 빈 문자열이며 오류가 아니다 |
| validate_direction | 번역 방향(원본→대상)이 지원 범위인지 검증한다 | sample: str — 원문 표본<br>target_lang: str — 대상 언어 코드<br>source_lang: str — 원본 언어 코드(비우면 표본으로 감지) | JSON 문자열 {ok, allowed, source_lang, target_lang, reason}. 거부는 오류가 아니라 allowed=false 판정이다 |
| list_languages | UI가 언어 선택지를 하드코딩하지 않게 목록을 제공한다 | 없음 | JSON 문자열. 지원 번역 언어 목록 |
| list_registers | UI가 문체 선택지를 하드코딩하지 않게 목록을 제공한다 | 없음 | JSON 문자열. 지원 문체(문어체/구어체) 목록 |
| resolve_register | 사용자가 고른 문체 값을 정규화한다 | register: str — 문체 값(비거나 알 수 없으면 기본값으로) | JSON 문자열 {ok, register, label, fell_back}. fell_back으로 기본값 대체를 알린다 |
| resolve_tone | 문서유형 정책에 따라 실제 적용할 톤을 확정한다 | doc_type: str — 문서유형 값(비우면 기본값)<br>tone: str — 사용자가 고른 톤(비우면 기본값) | JSON 문자열 {ok, tone, tone_label, tone_overridden, notice}. 강제 시 tone_overridden=true와 안내문을 함께 낸다 |

### 3-2. MCP 도구 — genon_text_guard.py

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| markdown_structure_issues | 원문과 되쓴 결과의 마크다운/HTML 구조를 대조해 표 행·열, 제목 단계, 코드펜스 훼손을 감지한다 | source: str — 원문<br>revised: str — 되쓴 결과 | JSON 문자열 {ok, issues, issue_count}. 되돌리지 않고 경고만 낸다 |
| fact_issues | 되쓴 글에서 숫자·날짜가 사라지거나 바뀌었는지 다중집합으로 대조한다 | source: str — 원문<br>revised: str — 되쓴 결과 | JSON 문자열 {ok, issues, issue_count, counts}. counts는 숫자/날짜를 나눠 센 값. 날짜는 표기가 달라도 같은 날이면 같다 |
| numeric_issues | 번역문의 숫자 보존을 확인한다. 자릿수 구분 기호를 떼고 비교한다 | source: str — 원문<br>revised: str — 번역 결과 | JSON 문자열 {ok, issues, issue_count, has_drift, drift}. drift는 문서 내용이므로 로그에 싣지 않는다 |
| diff_changes | 사용자에게 보여줄 문장 단위 변경 내역을 difflib으로 산출한다 | source: str — 원문<br>revised: str — 되쓴 결과<br>max_items: int — 최대 변경 건수(1~500, 기본 50) | JSON 문자열 {ok, changes, change_count}. LLM에 되묻지 않는다 |

### 3-3. MCP 도구 — genon_hwpx_text.py

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| hwpx_to_markdown | hwpx를 직접 파싱해 본문·표를 표 안 수치를 보존한 채 마크다운으로 변환한다 | content_base64: str — hwpx 바이트의 base64(볼륨 공유를 전제하지 않는 권장 경로)<br>path: str — 공유 볼륨 상의 hwpx 경로. content_base64가 있으면 그쪽이 우선<br>max_chars: int — 길이 상한(0이면 무제한, 기본 200000) | JSON 문자열 {ok, markdown, truncated, section_count}. 단순한 표는 마크다운, 병합(rowSpan/colSpan)·중첩 표는 HTML로 낸다 |

### 3-4. MCP 도구 — genon_glossary.py

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| glossary_lookup | 문장에 걸린 사내 용어와 그 지정 번역을 모아 낸다. 같은 용어가 여러 문장에 나와도 한 번만 낸다 | texts: list 또는 str — 검사할 문장들(최대 200개, JSON 배열 문자열도 허용)<br>target_lang: str — 번역 대상 언어 코드 | JSON 문자열 {ok, terms, term_count, enabled, reason}. enabled=false를 반드시 확인해야 한다 — 2단계 폴백이 없다 |
| glossary_status | 용어사전 적재 상태를 낸다. 번역 응답과 GET /glossary가 같은 값을 본다 | target_lang: str — 언어별 상태를 볼 때만 지정. 비우면 전체 적재 상태 | JSON 문자열 {ok, store{loaded, reason, languages}}. target_lang 지정 시 language, term_count, disabled 추가 |
| glossary_reload | 관리자가 용어를 등록·수정하고 승인 결재가 끝난 뒤 볼륨 파일을 재적재한다 | 없음 (호스트·드라이브를 인자로 받지 않는다 — 임의 호스트 호출이 된다) | JSON 문자열. 설정 미완료면 {ok: false, reason: "api_not_configured"} |

### 3-5. 워크플로우 스텝 — SFR-006

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| sfr006_01_context.run | 중간 스텝. 세션·변수로 템플릿을 확정하고 항목 목록·기존 값을 가져온다 | data.question — 사용자 발화<br>data.overrideConfig.vars.template_fill_template_id<br>data.session_id | dict. question, session_id, template_id, field_names, block_styles, field_values, blocks, fields_missing, ready_for_download, template_markdown, template_markdown_truncated, error |
| sfr006_02_extract.run | 중간 스텝. 발화를 코드서빙에 넘겨 LLM 추출 + 코드 판정 결과를 받는다 | data.question<br>data.template_id<br>data.session_id | dict. fields_updated, fields_cleared, fields_rejected, blocks_added, block_clears, error |
| sfr006_03_commit.run | 마지막 스텝. 값을 세션에 병합·저장하고 미리보기와 답변을 스트리밍한다 | 스텝2 산출 전부<br>data.socketIOClientId | async generator. token 이벤트 후 result 1회. data에 text, field_values, field_values_raw, fields_filled, fields_missing, ready_for_download, blocks, blocks_removed, document_markdown, document_markdown_truncated, error |

### 3-6. 워크플로우 스텝 — SFR-018 글다듬이

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| sfr018_polish_01_policy.run | 중간 스텝. 원본을 확보하고 문서유형·톤을 MCP로 확정한다 | vars.polish_doc_type<br>vars.polish_tone<br>vars.genosUploaded 또는 data.question | dict. polish_source_text, polish_doc_type, polish_tone, tone_overridden, tone_notice, error |
| sfr018_polish_02_polish.run | 마지막 스텝. 다듬기를 요청하고 구조·사실 대조 결과를 붙여 스트리밍한다 | data.polish_source_text<br>data.polish_doc_type<br>data.polish_tone | async generator. token 후 result 1회. data에 text(화면용), polished_text(파일용), changes, structure_warnings, fact_warnings, error |

### 3-7. 워크플로우 스텝 — SFR-018 번역

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| sfr018_translate_01_detect.run | 중간 스텝. 원본을 확보하고 번역 방향을 검증한다 | vars.translate_target_lang<br>vars.translate_source_lang<br>vars.translate_register<br>vars.translate_hwpx_path | dict. translate_source_text, translate_source_kind, translate_source_lang, translate_target_lang, translate_source_detected, translate_source_mismatch, translate_detected_lang, translate_glossary_applies, translate_register, error |
| sfr018_translate_02_translate.run | 마지막 스텝. 번역을 요청하고 숫자 보존 확인 결과를 붙여 스트리밍한다 | data.translate_source_text<br>data.translate_target_lang<br>data.translate_source_lang | async generator. token 후 result 1회. data에 text, translated_markdown(파일용), translated_markdown_highlighted(화면용), source_markdown, translate_source_kind, glossary, translate_pairs, translate_stats, numeric_warnings, error |

### 3-8. 워크플로우 스텝 — SFR-018 FAQ

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| sfr018_faq_01_source.run | 중간 스텝. 원본을 확보하고 배포 상한 안에서 생성 개수를 결정한다 | vars.faq_hwpx_path<br>vars.faq_count<br>vars.faq_max_count<br>vars.faq_title<br>vars.genosUploaded | dict. faq_source_text, faq_source_kind, faq_count, faq_effective_max, faq_title, faq_session_id, error |
| sfr018_faq_02_generate.run | 마지막 스텝. FAQ 생성을 요청하고 근거 검증 결과와 함께 스트리밍한다 | data.faq_source_text<br>data.faq_count<br>data.faq_session_id | async generator. token 후 result 1회. data에 text, faq_items, faq_stats, faq_download_ready, error |

### 3-9. 코드서빙 엔드포인트 — SFR-006_template_fill

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| GET /health, GET /, GET "" | 헬스체크·루트 | 없음 | 상태 dict |
| GET /templates | 등록된 템플릿 목록과 색인 상태를 낸다 | 없음 | templates, formats(항상 ["hwpx"]) |
| POST /templates | 관리자 등록. 업로드 후 즉시 색인한다(파싱 먼저, 파일 쓰기 나중) | multipart hwpx 파일<br>X-Admin-Token 헤더 | 등록 결과 + 슬롯 경고 목록 |
| DELETE /templates/{template_id} | 관리자 삭제. 색인도 함께 폐기한다 | template_id: str<br>X-Admin-Token 헤더 | 삭제 결과 |
| GET /fields | 항목 스키마와 본문 블록 서식 목록을 낸다 | template_id: str | field_names, block_styles |
| GET /status | 세션 채움 현황을 낸다(다운로드 버튼 활성화 판단용) | session_id: str<br>template_id: str | fields_filled, fields_missing, ready_for_download |
| GET /preview | 채운 결과를 마크다운으로 낸다(표시 전용) | session_id: str<br>template_id: str | document_markdown, document_markdown_truncated |
| PATCH /values | 화면에서 고친 항목 값을 반영한다 | session_id: str<br>template_id: str<br>values: dict<br>preview: bool | 갱신 결과 + 미리보기 |
| DELETE /values | 항목 값을 비운다 | session_id: str<br>template_id: str<br>fields: list<br>preview: bool | 갱신 결과 |
| PUT /blocks | 본문 추가 내용을 배열 통째로 교체한다(인덱스 어긋남으로 엉뚱한 문단을 지우는 것을 막는다) | session_id: str<br>template_id: str<br>blocks: list<br>preview: bool | 갱신 결과 |
| POST /generate | 등록 템플릿으로 초안을 생성해 내려준다 | template_id, session_id, values, filename, format, blocks | hwpx 바이트 + X-Missing-Fields, X-Written-Fields, X-Styled-Fields, X-Body-Blocks, X-Overflow-Fields, X-Open-Safety-Checked, X-Document-Format 헤더 |
| POST /generate/upload | 업로드한 hwpx로 즉석 생성한다 | multipart hwpx + 값 JSON | hwpx 바이트 + 동일 헤더 |
| POST /chat/context | 대화 1단계. 항목·기존 값·템플릿 미리보기를 낸다 | session_id: str<br>template_id: str | field_names, block_styles, field_values, blocks, template_markdown |
| POST /chat/extract | 대화 2단계. LLM 추출 후 코드가 검증한다 | session_id, template_id, question | fields_updated, fields_rejected, blocks_added, block_clears |
| POST /chat/commit | 대화 3단계. 병합·저장·미리보기를 한 요청으로 처리한다 | session_id, template_id, fields_updated, fields_cleared, fields_rejected, blocks_added, block_clears | field_values, fields_filled, fields_missing, document_markdown |

X-Open-Safety-Checked의 0은 통과가 아니라 미판정이다. 검사 없이 나간 파일을 검사 통과처럼 보이게 하지 않는다.

### 3-10. 코드서빙 엔드포인트 — SFR-018_text_polish

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| GET /health, GET /, GET "" | 헬스체크·루트 | 없음 | 상태 dict |
| GET /policies | 지원 정책 목록(내장 + 관리자 추가분)을 낸다 | 없음 | doc_types, tones, policy.source, policy.reason |
| POST /policies/reload | 관리자 정책을 재적재한다(재배포 없이 반영) | X-Admin-Token 헤더 | 갱신 결과 |
| POST /polish | 문서유형·톤 정책에 맞춰 원문을 다듬는다 | text: str — 원문<br>doc_type: str<br>tone: str | polished_text, doc_type, tone, tone_overridden |
| POST /download | 다듬은 본문을 txt 파일로 낸다. 이 단위는 상태를 갖지 않는다 | text: str 또는 polished_text: str(별칭)<br>title: str(최대 200자) | txt 바이트 (UTF-8 BOM + CRLF) |

/polish 응답의 polished_text를 되돌려 보내야 한다. 화면 표시용 text에는 경고문과 변경내역이 붙어 있어 파일에 섞이면 사용자가 메모장에서 지워야 한다.

### 3-11. 코드서빙 엔드포인트 — SFR-018_translation

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| GET /health, GET /, GET "" | 헬스체크·루트 | 없음 | 상태 dict |
| GET /languages | 지원 언어·문체 목록을 낸다(UI 선택지) | 없음 | languages, registers |
| GET /glossary | 용어사전 적재 상태를 낸다 | 없음 | loaded, languages, reason |
| POST /glossary/reload | 관리자가 용어사전을 재적재한다 | X-Admin-Token 헤더 | 재적재 결과 |
| POST /translate | 노드 목록을 번역한다 | nodes: list[dict]<br>target_lang: str<br>source_lang: str<br>register: str | nodes, glossary, stats |
| POST /translate/markdown | 전처리기 마크다운/HTML을 번역한다 | markdown: str<br>target_lang: str<br>source_lang: str<br>register: str | translated_markdown, translated_markdown_highlighted, source_markdown, glossary, pairs, stats |
| POST /translate/hwpx | hwpx를 직접 파싱한 뒤 번역한다(전처리기를 거치면 표 안 수치가 깨진다) | multipart hwpx + 언어 인자 | 위와 동일 |
| POST /download | 번역문을 txt 파일로 낸다. 상태 없음 | text: str 또는 markdown: str(별칭)<br>title: str | txt 바이트 (UTF-8 BOM + CRLF) |

### 3-12. 코드서빙 엔드포인트 — SFR-018_faq

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| GET /health, GET /, GET "" | 헬스체크·루트 | 없음 | 상태 dict |
| GET /config | 배포 상한·기본 개수·내려받을 수 있는 형식을 낸다 | 없음 | max_count, default_count, formats(항상 ["txt"]) |
| POST /generate | 마크다운 본문으로 FAQ를 생성하고 근거를 검증한다 | markdown: str<br>count: int (0~1000, 0이면 기본 개수)<br>session_id: str<br>title: str | items, stats(기각 건수 schema/ungrounded/duplicate 포함) |
| POST /generate/upload | hwpx 업로드를 직접 파싱한 뒤 생성한다 | multipart hwpx + 인자 | 위와 동일 |
| GET /faqs | 세션에 저장된 FAQ를 조회한다 | session_id: str<br>X-Admin-Token 헤더 | items |
| POST /download | 저장된 FAQ를 txt로 낸다. 다시 생성하지 않으며 세션을 지우지도 않는다 | format: str(기본 txt, 옛 hwpx/pdf/xlsx는 400)<br>session_id: str<br>items: list[dict]<br>title: str | txt 바이트 (UTF-8 BOM + CRLF) |

### 3-13. 전처리기 (area 05)

| 함수명 | 목적 | 입력 파라미터 | 출력 파라미터 |
|---|---|---|---|
| parse | hwpx 바이트를 블록 목록으로 파싱한다 | hwpx_bytes: bytes | Document(blocks, section_count) |
| annotate_outline | 조문 위계를 주석한다(조문 문서일 때만 켜진다) | blocks: list[Block] | list[Block] |
| chunk_blocks | 블록 경계를 지키며 청킹한다 | blocks: list[Block] | list[Chunk] |
| to_records | VDB 적재용 레코드를 만든다 | chunks: list[Chunk]<br>file_name: str<br>section_count: int | list[dict] |
| DocumentProcessor.\_\_call\_\_ | GenOS가 실제로 부르는 진입점 | request<br>file_path<br>chunk_size: int (기본 1000)<br>chunk_overlap: int (기본 100) | list[dict] — VDB 레코드 |

전처리기는 병합이 없는 표도 언제나 HTML로 낸다. RAG 검색 결과가 프롬프트로 조립될 때 개행이 뭉개져 마크다운 표가 통째로 무너지기 때문이다. 등록 후 관리 화면에서 hwpx 업로드를 이쪽으로 매핑해야 실제로 쓰인다.

### 3-14. 의도된 중복 — 건드리지 말 것

배포 단위 간 import가 금지돼 있어 강제된 사본이 있다. 문서가 아니라 출력으로 대조한다.

| 사본 | 벌 수 | 지키는 점검 |
|---|---|---|
| 표 격자 규칙 (cellAddr 좌표·병합 앵커) | 4 | check_table_grid.py |
| 톤 프리셋 문구 | 3 | check_tone_policy.py |
| 워크플로우 스텝의 로깅·오류표·게이트웨이 클라이언트 | 9 | check_deploy_contract.check_workflow_steps() |
| txt_output.py (BOM·CRLF·헤더·파일명) | 3 | check_unit_endpoints.py |

---

## 4. AI 응답 시나리오

### 4-1. 시나리오 목록

| 단계-번호 | 시나리오명 | 분석 유형 | 목적 | 우선 순위 |
|---|---|---|---|---|
| S1-01 | 템플릿 항목 추출 | 생성형 + 구조 검증 | 사용자 발화에서 항목명·값을 뽑아 hwpx 슬롯에 매핑한다 | 최상 |
| S1-02 | 항목 삭제·본문 블록 판별 | 생성형 + 화이트리스트 | 발화에 섞인 수정·삭제·본문 추가 의도를 분리한다 | 상 |
| S1-03 | 채움 완료 판정 | 결정적 | ready_for_download를 산출해 캔버스 분기 근거로 쓴다 | 최상 |
| S1-04 | 서식 적용 | 결정적 | 슬롯 인자(크기·글꼴·굵게)를 charPr 복제로 적용한다 | 중 |
| S2-01 | 문서유형 톤 확정 | 결정적 (MCP) | 정책 강제 여부까지 판정하고 강제 시 사용자에게 고지한다 | 상 |
| S2-02 | 문체 다듬기 | 생성형 | 문서유형·톤 정책에 맞춰 한국어 원문을 개선한다 | 최상 |
| S2-03 | 구조 훼손 감지 | 결정적 (지문 대조) | 표 행·열, 제목 단계, 코드펜스 보존을 확인한다 | 최상 |
| S2-04 | 사실 보존 검증 | 결정적 (다중집합) | 숫자·날짜 소실 및 변조를 감지한다 | 최상 |
| S2-05 | 변경 내역 산출 | 결정적 (difflib) | 무엇이 어떻게 바뀌었는지 문장 단위로 제시한다 | 상 |
| S3-01 | 번역 방향 검증 | 결정적 (문자 체계) | 지원 범위 밖 요청을 사전에 거부한다 | 최상 |
| S3-02 | 스켈레톤 분해·재조립 | 결정적 (파서) | 구조를 LLM 출력과 무관하게 원본과 동일하게 유지한다 | 최상 |
| S3-03 | 문장 번역 | 생성형 | 셀·문장 텍스트만 대상 언어로 변환한다 | 최상 |
| S3-04 | 용어사전 준수율 재계산 | 결정적 (대조) | 프롬프트 지시로 끝내지 않고 코드가 다시 센다 | 상 |
| S3-05 | 숫자 보존 확인 | 결정적 (지문) | 자릿수 기호 차이를 오탐으로 보지 않고 대조한다 | 상 |
| S4-01 | FAQ 문답 생성 | 생성형 | 문서에서 질문·답변·근거 문장을 추출한다 | 최상 |
| S4-02 | 근거 정합성 검증 | 결정적 (n-gram) | 근거가 실제로 원문에 있는지 대조하고 없으면 기각한다 | 최상 |
| S4-03 | 중복·스키마 기각 | 결정적 | 형식 불량과 중복 문항을 제거하고 기각 건수를 노출한다 | 상 |
| S4-04 | 개수 상한 조정 | 결정적 | 배포 상한과 캔버스 상한, 사용자 요청의 교집합을 취한다 | 중 |
| S5-01 | hwpx 표 구조 판별 | 결정적 (XML) | 병합·중첩 유무로 마크다운과 HTML 출력을 분기한다 | 최상 |
| S5-02 | RAG 청킹 | 결정적 | 블록 경계를 지켜 표가 잘리지 않게 분할한다 | 상 |

### 4-2. 상세 시나리오 정의

#### S1-01 템플릿 항목 추출

| 항목 | 내용 |
|---|---|
| 분류 | SFR-006 / 생성형 + 구조 검증 |
| 트리거 | 사용자 발화가 비어 있지 않은 턴 (sfr006_02_extract) |
| 입력 | question(발화, 최대 20,000자), field_names(템플릿 항목 화이트리스트), block_styles |
| LLM 책임 | 발화에서 항목명·값, 지울 항목, 본문 블록을 추출하는 것까지만 |
| 코드 책임 | 화이트리스트 검증, 값 보존 확인, 서식 이름 검증. 전부 결정적이다 |
| 출력 | fields_updated, fields_cleared, fields_rejected, blocks_added, block_clears |
| 기각 조건 | 템플릿에 없는 항목명(환각), 값 길이 초과(2,000자), 미등록 서식 이름 |
| 예외 처리 | 발화가 비면 LLM을 부르지 않는다. 빈 발화로 부르면 항목을 지어낸다 |
| 성능 지표 | 항목별 precision·recall·F1, 값 정확도(정규화 후 exact match), 환각률 |
| 로그 | event=extract_done, item_count, 기각 건수. 값 자체는 남기지 않는다 |

#### S2-03 구조 훼손 감지

| 항목 | 내용 |
|---|---|
| 분류 | SFR-018 글다듬이 / 결정적 (MCP text_guard) |
| 트리거 | POST /polish 응답 직후 (sfr018_polish_02_polish) |
| 입력 | source(원문), revised(다듬은 결과) |
| 판정 방식 | 다듬기 전후의 구조 지문을 대조한다. 표 행·열 수, 제목 단계, 코드펜스 |
| 출력 | {ok, issues(고정 안내문 목록), issue_count} |
| 조치 | 되돌리지 않고 경고만 낸다. 자동 롤백은 정상적인 표기 정리까지 되돌린다 |
| 왜 감지 방식인가 | 글다듬이는 문장 문맥이 필요해 문서를 통째로 LLM에 보낸다. 번역처럼 스켈레톤을 분리할 수 없으므로 사후 대조가 유일한 방어선이다 |
| 성능 지표 | 지문 대조 통과율 (markdown_structure_issues 기준) |
| 사본 주의 | 톤 프리셋 사본 3벌이 실제로 갈린 적이 있다 (006 friendly 한 문장 누락) |

#### S3-02 스켈레톤 분해·재조립

| 항목 | 내용 |
|---|---|
| 분류 | SFR-018 번역 / 결정적 (파서) |
| 트리거 | POST /translate/markdown 진입 시 |
| 입력 | 전처리기 마크다운 또는 hwpx 직접 파싱 결과 |
| 처리 | 표 파이프·HTML 태그·제목·목록·코드펜스를 코드가 스켈레톤으로 분리하고, LLM에는 셀·문장 텍스트만 보낸다 |
| 계약 1 무손실 | 항등 번역이면 산출물이 입력과 문자 단위로 같다 |
| 계약 2 구조 불변 | 번역 후에도 줄마다 파이프 수와 마커가 원본과 같다 |
| 부수 규칙 1 | 번역문의 파이프 문자를 이스케이프한다. 안 하면 그 행부터 열이 밀린다 |
| 부수 규칙 2 | 번역문 줄바꿈을 정규화한다. 줄바꿈 하나에 표 행이 갈라진다 |
| 부수 규칙 3 | 같은 원문은 한 번만 호출한다. 비용도 줄고 반복 머리글 흔들림도 사라진다 |
| 부수 규칙 4 | 번역할 텍스트가 없는 문서(숫자 표)는 LLM을 아예 부르지 않는다 |
| 출력 | translated_markdown, stats.fallback_rate |
| 성능 지표 | 재조립 실패·세그먼트 수 불일치로 인한 fallback 발생률(0에 수렴해야 함) |
| 검증 | SFR-018/tests/test_markdown_units.py |

#### S4-02 근거 정합성 검증

| 항목 | 내용 |
|---|---|
| 분류 | SFR-018 FAQ / 결정적 (faq/evidence.py) |
| 트리거 | LLM이 문답과 evidence를 반환한 직후 |
| 입력 | evidence(LLM이 제시한 근거 문장), 원천 문서 본문 |
| 판정 방식 | 완전 포함이면 1.0, 아니면 3-gram 겹침 비율을 FAQ_EVIDENCE_MIN_RATIO와 비교한다 |
| 출력 | 채택 항목 + stats(schema, ungrounded, duplicate 기각 건수) |
| 왜 필요한가 | 검증 없이 표시만 하면 근거란이 장식이 된다. 지어낸 답변에 그럴듯한 출처가 붙으면 더 위험하다 |
| 침묵 금지 | 기각 건수를 전부 노출한다. 조용히 버리면 5개 요청에 3개만 나온 이유를 알 수 없다 |
| 성능 지표 | 기각률과 유형별 분포 |
| 주의 | 어휘 중복이 낮다고 곧장 오답은 아니다. 재서술 가능성이 있으므로 임계 미달 건에만 확인을 붙인다 |

#### S5-01 hwpx 표 구조 판별

| 항목 | 내용 |
|---|---|
| 분류 | 공통 / 결정적 (XML 파싱) |
| 트리거 | hwpx_to_markdown 호출 또는 전처리기 적재 |
| 판정 기준 | rowSpan·colSpan 존재 여부, 중첩 표 존재 여부 |
| 출력 분기 | 손실 없으면 마크다운 표, 손실 있으면 HTML table |
| 왜 갈리는가 | 마크다운 표에는 병합 문법이 없다. 병합 셀은 빈 칸이 되고 중첩 표는 한 덩어리 텍스트로 뭉개진다. 수치는 남는데 그 수치가 무엇의 값인지가 사라진다 |
| 구현 주의 1 | 셀 텍스트 추출 시 tc.iter(hp:p)를 쓰면 중첩 표 안 문단까지 딸려온다. _owning_cell로 소유 셀을 따진다 |
| 구현 주의 2 | 덮인 자리에 td를 내지 않는다. 내면 그 행만 열이 하나 늘어난다 |
| 예외 1 | 전처리기(area 05)는 병합이 없는 표도 언제나 HTML로 낸다 |
| 예외 2 | 006 hwpx_markdown.py는 제외한다. 채팅 미리보기용이라 마크다운이 맞다 |
| 사본 수 | 4벌 (check_table_grid.py가 대조) |

### 4-3. 제한 사항 및 유의사항

#### (1) 아직 실물로 확인되지 않은 것

LLM 게이트웨이, Redis, 한/글 실물이 있어야만 확인되는 항목이다.

| 미확인 항목 | 영향 | 실패 시 대응 |
|---|---|---|
| LLM 실호출 경로 전체 | 게이트웨이가 없어 프롬프트 한국어·영어 분리의 실제 효과가 미확인 | 실물 확보 후 재검증 |
| 게이트웨이의 JSON-RPC 통과 여부 | MCP 호출 규약이 다르면 전부 실패 | 스텝 9개의 _mcp_call을 각각 수정 (자기완결 규율상 공용 모듈로 못 뺀다) |
| MCP 파일 등록 성립 여부 | 파일 4개를 올려 도구 14개가 다 뜨는지 미확인 | 등록 화면 확인 후 조정 |
| 생성 hwpx의 한/글 개봉 | 확인할 한/글이 없고 개봉 안전 게이트도 2026-08-12에 제거됨 | reopen_checked=False로 하지 않았다고 말한다 |
| 빌드·시작 커맨드의 셸 경유 | cd A && B 형식이 안 먹을 수 있음 | uvicorn --app-dir로 변경 |
| 워크플로우 스텝 간 data 크기 한도 | 큰 문서에서 실패 가능 | 본문 대신 핸들(세션 키)만 넘기는 형태로 변경 |
| 번역 02 스텝 2개 | 코드는 있고 실행도 되지만 캔버스에 등록된 적이 없다 | 등록 후 재검증 |
| 실제 사내 용어사전 파일 | _MAX_TERM_WORDS=6과 캐시 상한 30만 건이 실물에 맞는지 미검증 | 실물 확보 후 조정 |
| FAQ hwpx 템플릿 실물 | 반복 블록 규약에 맞는 사내 서식 파일이 없다 | 실물 확보 후 검증 |

#### (2) 기능적 제한

| 항목 | 제한 내용 |
|---|---|
| 번역 언어 쌍 | 6개 언어. 원본이나 대상 중 하나는 반드시 한국어여야 한다. en→ru는 400 |
| 용어사전 2단계 부재 | Weaviate + 임베딩 경로는 폐쇄망 벡터DB 가용성 미확인으로 보류. 폴백이 없으므로 사전이 없거나 상한 초과면 그 언어는 용어사전 없이 번역되고 glossary.source로만 드러난다 |
| 태국어·중국어 용어 매칭 | 띄어쓰기가 없어 토큰이 길게 잡히므로 사실상 완전 일치만 걸린다 |
| 006 산출 형식 | hwpx 하나. format은 계속 받지만 hwpx 외의 값은 400이다. 조용히 hwpx를 주면 화면은 PDF를 받았다고 믿는다 |
| 018 산출 형식 | txt 하나. hwpx·pdf·xlsx 내보내기는 전부 제거됨 |
| 스트리밍 | 실시간 토큰 스트리밍이 아니다. LLM 응답을 다 받은 뒤 32자씩 잘라 emit한다 |
| 평가 도구 | 임베딩·LLM Judge 평가는 온프레미스 서빙 가용성 확인 후 착수. 미구현 사실이 metric_catalog의 not_implemented로 노출된다 |
| 개봉 안전 게이트 부재 | 2026-08-12에 제거됨. 남은 hwpx 산출 경로는 006 하나뿐이고 check_output_safety.py가 파트 선언과 누름틀 안내문만 본다 |

#### (3) 입력 상한

| 대상 | 환경변수 | 기본값 |
|---|---|---|
| 006 업로드 템플릿 | TEMPLATE_FILL_MAX_UPLOAD_BYTES | 20MB (전량 메모리 파싱) |
| 006 항목 수 | TEMPLATE_FILL_MAX_FIELDS | 200 |
| 006 값 길이 | TEMPLATE_FILL_MAX_VALUE_CHARS | 2,000 |
| 006 발화 길이 | TEMPLATE_FILL_MAX_MESSAGE_CHARS | 20,000 |
| 006 본문 블록 개수·길이 | TEMPLATE_FILL_MAX_BLOCKS / _MAX_BLOCK_CHARS | 100 / 4,000 |
| 006 미리보기 | TEMPLATE_FILL_MAX_PREVIEW_CHARS | 20,000 |
| 글다듬이 입력 | POLISH_MAX_INPUT_CHARS | 200,000 (넘으면 자르지 않고 거절) |
| 번역 입력 | TRANSLATE_MAX_NODES / _MAX_TOTAL_CHARS / _MAX_UPLOAD_BYTES | 배포별 설정 |
| FAQ 개수 | FAQ_MAX_COUNT / FAQ_DEFAULT_COUNT | 10 / 5 |
| FAQ 컨텍스트·업로드 | FAQ_MAX_CONTEXT_CHARS / FAQ_MAX_UPLOAD_BYTES | 배포별 설정 |
| FAQ 근거 임계 | FAQ_EVIDENCE_MIN_RATIO | 배포별 설정 |
| hwpx 마크다운 길이 | max_chars 인자 | 200,000 (0이면 무제한) |
| 용어사전 조회 문장 수 | glossary_lookup texts | 200개 |

개수 상한은 두 층이다. 배포 상한(FAQ_MAX_COUNT) 안에서만 캔버스 변수(faq_max_count)로 낮출 수 있다. 캔버스가 상한을 넘길 수 있으면 LLM 예산 상한이 설정 하나로 무력해진다.

#### (4) 개발·운영 유의사항

| 항목 | 내용 |
|---|---|
| 배포 단위 간 import 금지 | GenOS 등록 단위가 독립 컨테이너·파일이다. 의도된 사본이 존재하며 갈렸는지는 점검이 출력으로 대조한다 |
| 워크플로우 스텝을 쪼개지 않는다 | 캔버스 파이썬 스텝은 코드 한 덩어리로 등록된다. 로깅·오류표·게이트웨이 클라이언트 중복은 의도한 것이다 |
| 워크플로우 패키지는 httpx만 | lxml·redis·jinja2는 전부 코드서빙 쪽에 있다. 워크플로우 이미지에 추가되는 패키지가 0개인 것이 2026-08-11 재배치의 결과다 |
| 세션 저장은 덮어쓰기 | 값만 저장하면 블록이 지워진다. 항상 함께 넘긴다 |
| 슬롯 규칙 변경 시 SCHEMA_VERSION | 인식 규칙이나 FieldSpec을 고치면 반드시 올린다. 캐시 무효화 근거다 |
| 프롬프트는 배포 단위 밖 | onprem/prompt/<단위명>/*.j2. 이미지에 함께 넣어야 한다. 없으면 빈 프롬프트로 넘어가지 않고 요청을 세운다 |
| 프롬프트 디렉토리는 상위로 훑는다 | 고정 깊이로 찾다가 단위가 한 겹 내려가자 네 단위의 프롬프트가 동시에 사라진 적이 있다 |
| 지시문 언어 분리 | 구조·형식·금지 조항은 영어, 산출물의 언어·어투·표기 규칙은 한국어. 번역 단위만 전부 영어다 |
| emit 뒤 sleep(0) 필수 | 양보하지 않고 몰아치면 소켓 쓰기가 버퍼에 쌓여 UI가 마지막에 한꺼번에 받는다 |
| data를 통째로 갈지 않는다 | {**data, ...} 형태로 반환한다. 통째로 갈면 genos_state(trace_id)를 잃는다 |
| 재시도 판정을 스텝이 덮어쓰지 않는다 | 상태코드가 아니라 응답 본문 error_code 분류(00020003)로 본다. 상태코드만 보면 서빙의 재시도 무의미 판정이 경계에서 사라지고, 사용자는 몇 번을 눌러도 같은 자리에서 실패하는 문제에 잠시 후 다시 시도하라는 안내를 반복해서 본다 |
| 전처리기 매핑 필수 | 등록만으로는 안 된다. 관리 화면에서 hwpx 업로드를 전처리기로 매핑해야 실제로 쓰인다 |
| Windows 콘솔 | PYTHONIOENCODING=utf-8 필수. cp949가 em dash에서 죽는다 |

#### (5) 로그 규약

| 규칙 | 내용 |
|---|---|
| 화이트리스트 | event, trace_id, request_id, resource_id, status, duration_ms, item_count, upstream_status, error_code, error_type만 허용한다 |
| 값 전달 | 반드시 extra로 넘긴다. 메시지에 f-string으로 끼워 넣으면 화이트리스트가 무력해진다 |
| 문서 내용 금지 | numeric_issues의 drift처럼 문서 내용을 담는 값은 로그에 싣지 않는다 |
| 사용자 노출 문구 | 각 파일에서 쓴 고정 한국어 안내문만 담는다. 예외 원문은 error_type(클래스 이름)으로 로그에만 남긴다 |
| 허용 외 필드 | 값은 버리고 이름만 dropped_fields로 남긴다 |

#### (6) 오류 코드 체계

| 영역 | 코드 접두 | 반환 방식 |
|---|---|---|
| 워크플로우 | 02- | data["error"] = {error_code, retryable, msg} |
| 코드서빙 | 03- | {error_code, msg} JSON. 006은 ApiError 예외 하나로 올려 핸들러가 변환한다 |
| MCP | 없음 | {"content": [...], "isError": true} |
| eval | 없음 | 로그를 남긴 뒤 예외. 로그는 stderr 전용 |

공통 코드 3종만 조합한다. 00020001은 통신 실패, 00020002는 실행 실패, 00020003은 그 외다. 원인 구분은 error_type과 user_msg로 한다.

---

## 5. 검증 현황

서버·Redis·LLM 없이 전부 돈다. 가짜는 배포 단위 밖에서 주입한다. 운영 코드에 테스트용 분기를 만들지 않기 위해서다.

| 점검 | 대상 | 건수 |
|---|---|---|
| SFR-006 unittest | 006 함수 단위 회귀 | 32 |
| SFR-018 unittest | 018 함수 단위 회귀 (전처리기 80건 포함) | 172 |
| check_deploy_contract.py | 빌드·기동 계약 | FAIL 0 / WARN 3 / OK 63 |
| check_service_boot.py | 코드서빙 4단위 실제 기동 | 16 |
| check_workflow_run.py | 워크플로우 스텝 9개 실행 | 74 |
| check_mcp_tools.py | MCP 도구 파일 4개 공존·결정적 판정 | 68 |
| check_api_contract.py | 006 엔드포인트 | 45 |
| check_chat_turn.py | 대화 한 턴 (02와 03 경계) | 22 |
| check_unit_endpoints.py | 018 세 단위 엔드포인트 + txt 규약 | 66 |
| check_body_blocks.py | 문단 복제 안전장치 | 17 |
| check_output_safety.py | 파트 선언·누름틀 안내문 | 5 |
| check_table_grid.py | 표 격자 사본 4벌 대조 | 18 |
| check_tone_policy.py | 톤 프리셋 사본 3벌 대조 | 22 |
| 합계 | unittest 204 + 점검 416 | 620 |

2026-08-18 기준 전부 통과했다. 이 숫자가 곧 회귀 감지 기준이므로 점검을 고칠 때 test/README.md, HANDOFF.md 3-1절, FEATURES.md 세 곳을 같이 고친다. 남은 WARN 3은 의도된 것으로, try/except ImportError로 방어된 fastmcp와 루트 main.py가 없어 시작 커맨드가 필수인 두 단위다. check_unit_endpoints는 SSL_CERT_FILE이 없는 경로를 가리키면 2건 실패하는데 코드 결함이 아니다.

# 3-2. 상세 시나리오 정의 (SFR-018)

이 장은 글다듬이·번역·FAQ 생성 세 기능이 **실제로 어떤 순간에, 무엇을 보고, 어떻게 판단해서, 무엇을 쓰는지**를 시나리오 단위로 적는다. 개발자가 아닌 독자를 위해 각 시나리오 첫 줄에 "쉽게 말하면"을 붙였다.

**공통 전제 3가지**

| 전제 | 내용 |
|---|---|
| 판정은 AI가 하지 않는다 | AI(LLM)는 **문장을 만드는 일**만 한다. "표가 깨졌는가 / 숫자가 사라졌는가 / 근거가 진짜인가" 같은 **합격·불합격 판정은 전부 프로그램 코드가 계산**한다. 같은 입력에 항상 같은 답이 나와야 검증이 성립하기 때문이다 |
| 조용히 넘어가지 않는다 | 값을 깎았거나, 항목을 버렸거나, 검사를 못 했으면 **그 사실을 반드시 화면에 표시**한다. 5개를 요청했는데 3개만 나왔다면 왜 그런지가 함께 나온다 |
| LLM 모델은 코드에 적지 않는다 | 어떤 모델을 쓸지는 `LLM_MODEL_ID` 환경변수로 배포할 때 정한다. 폐쇄망 고객사마다 반입된 모델이 다르기 때문이다. 호출 통로는 **GenOS 게이트웨이의 OpenAI 호환 경로** 하나뿐이다 |

---

## 가. SFR-018 글다듬이

### S2-01 · 문서유형에 따른 톤 확정

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | 사용자가 "공고문을 친근한 말투로 바꿔줘"라고 했을 때, 공고문은 친근한 말투를 쓰면 안 되는 문서다. 그럴 때 말투를 정중체로 되돌리고 **"바꿨습니다"라고 알려주는** 단계다 |
| **목적** | 문서유형별 정책에 따라 실제 적용할 톤을 확정한다. 정책이 사용자 선택을 덮어썼다면 그 사실을 사용자에게 고지한다 |
| **트리거 조건** | 사용자가 글다듬이 캔버스에서 문서를 업로드하거나 텍스트를 입력하고 실행한 직후. 워크플로우 첫 스텝(`sfr018_polish_01_policy`)이 시작될 때 자동 실행된다. 사용자가 별도로 요청하지 않는다 |
| **사용자 질의 예시** | · "이 사내 공고문 좀 다듬어줘"<br>· "보고서인데 문어체로 정리해줘"<br>· "이메일 초안인데 좀 더 친근하게"<br>· (문서만 첨부하고 아무 말도 없는 경우 — 기본 문서유형·기본 톤으로 진행) |
| **입력 조건** | · 문서유형(`polish_doc_type`) — 캔버스 드롭다운 값. 비어 있으면 기본값<br>· 사용자 선택 톤(`polish_tone`) — 비어 있으면 기본값<br>· 원본 텍스트 — 업로드 파일(`genosUploaded`) 또는 채팅 입력(`question`) 중 하나. **둘 다 없으면 진행하지 않고 안내문을 낸다**<br>· 길이 상한 200,000자. **넘으면 자르지 않고 거절한다** (잘린 문서를 다듬어 돌려주면 뒷부분이 통째로 사라진 결과가 정상 응답처럼 나간다) |
| **분석·판단 기준** | ① 문서유형이 등록된 목록에 있는가 → 없으면 기본값으로 대체하고 `fell_back` 표시<br>② 그 문서유형에 **톤 강제 규칙**이 걸려 있는가 → 걸려 있으면 사용자 선택을 무시하고 정책 톤으로 확정<br>③ 강제가 일어났으면 `tone_overridden=true`와 사용자용 안내 문구를 함께 낸다<br>④ 판단에 AI를 쓰지 않는다. 등록된 정책 표를 조회하는 **조회·대조 작업**이다 |
| **수행 내용** | 1. 업로드 파일 또는 채팅 텍스트에서 원본 확보<br>2. 길이 상한 검사 (초과 시 거절)<br>3. MCP `resolve_tone` 호출 — 문서유형·톤을 넘겨 최종 톤을 받는다<br>4. 강제 발생 시 안내문(`tone_notice`)을 다음 스텝에 실어 보낸다<br>5. 결과를 `dict`로 다음 스텝에 전달 |
| **사용 tool / Function** | **MCP 도구**: `genon_lang_policy.resolve_tone`, `genon_lang_policy.list_registers`<br>**HTTP 통신**: `httpx` (워크플로우 스텝은 이 패키지 하나만 쓴다)<br>**정책 저장소**: GenOS 프롬프트 라이브러리(admin-api). 관리자가 JSON을 등록하면 **재배포 없이** 문서유형·톤이 추가된다. 조회 실패 시 내장 기본값(`tone_presets.py`)으로 돌고 그 사실이 `GET /policies`의 `source`·`reason`에 드러난다<br>**LLM 모델**: 사용하지 않음 |

---

### S2-02 · 문체 다듬기 (본 작업)

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | 실제로 AI가 글을 고치는 단계다. 문서를 **통째로** 넘긴다 — 앞뒤 문장을 봐야 자연스럽게 고칠 수 있기 때문이다 |
| **목적** | 확정된 문서유형·톤 정책에 맞춰 한국어 원문의 문체·어투·표기를 개선한다. 내용을 바꾸는 게 아니라 **표현을 다듬는** 작업이다 |
| **트리거 조건** | S2-01이 오류 없이 끝나고 두 번째 스텝(`sfr018_polish_02_polish`)이 시작될 때. 앞 스텝에서 `data["error"]`가 세팅됐으면 **LLM을 부르지 않고 통과**한다 |
| **사용자 질의 예시** | · "이 회의록 문어체로 정리해줘"<br>· "너무 딱딱한데 좀 부드럽게 바꿔줄래?"<br>· "맞춤법이랑 어색한 표현만 고쳐줘" |
| **입력 조건** | · `polish_source_text` — 원문 전체<br>· `polish_doc_type`, `polish_tone` — S2-01이 확정한 값<br>· 문서 전체를 한 번에 보내므로 응답 대기 90초(`RES_TIMEOUT`), 재시도 2회(`LLM_RETRY_COUNT`) |
| **분석·판단 기준** | · **모델 온도(temperature) 0.3** — 번역과 같고 FAQ(0.2)보다 높다. 문체 개선은 어느 정도 표현의 자유가 필요하지만, 내용을 창작해서는 안 되므로 낮게 묶어 둔다<br>· 프롬프트 지시문의 **구조·형식·금지 조항은 영어**로, **산출물의 어투·표기 규칙은 한국어**로 쓴다. 한국어 어투 규칙을 영어로 적으면 모델이 규칙 자체를 번역해 이해하며 어긋난다<br>· 스트리밍을 쓰지 않는다(`stream: false`). 응답을 다 받은 뒤 검증을 거쳐야 하기 때문이다 |
| **수행 내용** | 1. 프롬프트 템플릿(`onprem/prompt/SFR-018_text_polish/*.j2`)을 문서유형·톤 값으로 렌더링<br>2. GenOS 게이트웨이 OpenAI 호환 경로로 `POST /polish` → LLM 호출<br>3. 실패 시 재시도(연결 3초, 읽기 90초로 나눠 잡음 — 연결은 빨리 포기하고 생성은 길게 기다린다)<br>4. 결과를 **검증(S2-03/04/05)에 넘긴다**<br>5. 화면용 `text`와 파일용 `polished_text`를 **분리해서** 반환 |
| **사용 tool / Function** | **LLM 모델**: `LLM_MODEL_ID` 환경변수로 지정된 폐쇄망 반입 모델. 코드에 모델명을 적지 않는다<br>**호출 경로**: GenOS Gateway OpenAI 호환 `/chat/completions`<br>**라이브러리**: `httpx`(비동기 HTTP), `jinja2`(프롬프트 템플릿 렌더링), `fastapi`+`pydantic`(요청·응답 스키마 검증)<br>**파라미터**: temperature 0.3, stream false, timeout(connect 3s / read 90s / write 5s) |

> **화면용과 파일용을 나눈 이유**: 화면에 뜨는 `text`에는 경고문("⚠ 표 구조가 …")과 변경내역 표가 붙어 있다. 이게 그대로 파일에 들어가면 사용자가 메모장에서 손으로 지워야 한다. 다운로드는 순수한 `polished_text`만 보낸다.

---

### S2-03 · 구조 훼손 감지

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | AI가 글을 고치다가 **표를 깨먹거나 제목 단계를 흐트러뜨렸는지** 확인하는 단계다. 발견해도 되돌리지는 않고 "여기 이상합니다"라고 알려만 준다 |
| **목적** | 다듬기 전후의 문서 골격이 같은지 대조해 표 행·열, 제목 단계, 코드블록 훼손을 잡아낸다 |
| **트리거 조건** | S2-02의 LLM 응답을 받은 **직후**, 사용자에게 보여주기 **전**. 사실 검증·변경내역과 함께 `asyncio.gather`로 동시에 부른다 (셋은 서로 독립이라 순서대로 기다릴 이유가 없다) |
| **사용자 질의 예시** | 사용자가 직접 요청하지 않는다. 모든 글다듬이 실행에 자동으로 붙는 **안전장치**다.<br>다만 결과는 사용자에게 이렇게 보인다 — "⚠ 표의 열 수가 원본과 다릅니다. 확인이 필요합니다." |
| **입력 조건** | · `source` — 다듬기 **전** 원문<br>· `revised` — 다듬기 **후** 결과<br>· 둘 다 문자열. 하나라도 비면 검사를 건너뛴다 |
| **분석·판단 기준** | 원문과 결과에서 각각 **구조 지문(fingerprint)**을 뽑아 비교한다.<br>· 표: 줄마다 파이프(`\|`) 개수, 전체 행 수, 열 수<br>· 제목: `#` 개수로 표현되는 단계 순서<br>· 코드블록: 백틱 세 개(``` )의 열림·닫힘 짝<br>· HTML 표: `<tr>`·`<td>` 개수<br>**하나라도 어긋나면 issue로 기록**한다. 판정에 AI를 쓰지 않는다 |
| **수행 내용** | 1. MCP `markdown_structure_issues`에 원문·결과를 함께 전달<br>2. 구조 지문 대조 → `issues` 목록과 `issue_count` 수신<br>3. **되돌리지 않는다.** 고정 안내 문구를 만들어 화면용 `text` 앞에 붙인다<br>4. 검증 자체가 실패해도(MCP 통신 오류 등) **다듬은 결과 전달을 막지 않는다** — 검증은 부가 정보이지 결과의 전제 조건이 아니다 |
| **사용 tool / Function** | **MCP 도구**: `genon_text_guard.markdown_structure_issues`<br>**라이브러리**: `re`(정규식으로 표·제목·코드펜스 탐지), `asyncio`(3종 검증 병렬 호출)<br>**LLM 모델**: 사용하지 않음 |

> **왜 되돌리지 않는가**: 자동 롤백을 넣으면 AI가 정상적으로 정리한 표기(불필요한 빈 줄 제거 등)까지 함께 되돌아간다. 사용자가 눈으로 확인하고 판단하는 편이 낫다.
> **왜 번역처럼 구조를 미리 떼어내지 못하는가**: 글다듬이는 문장 문맥이 필요해 문서를 통째로 보낸다. 번역처럼 표 골격을 미리 분리할 수 없으므로 **사후 대조가 유일한 방어선**이다.

---

### S2-04 · 사실 보존 검증 (숫자·날짜)

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | "예산 3,200만원"이 "예산 3,200만 원"이 되는 건 괜찮지만, "320만원"이 되면 큰일이다. **숫자와 날짜가 사라지거나 바뀌었는지** 세어보는 단계다 |
| **목적** | 문체를 다듬는 과정에서 사실 정보(수치·날짜)가 소실되거나 변조되지 않았는지 확인한다 |
| **트리거 조건** | S2-03과 동시. LLM 응답 직후 자동 실행 |
| **사용자 질의 예시** | 직접 요청하지 않는다. 결과는 "⚠ 원문에 있던 날짜 1건이 결과에서 확인되지 않습니다" 형태로 표시된다 |
| **입력 조건** | · `source`(원문), `revised`(결과) |
| **분석·판단 기준** | **다중집합(multiset) 대조** 방식이다. "어떤 숫자가 몇 번 나오는가"를 원문과 결과에서 각각 세어 비교한다.<br>· 순서가 바뀐 것은 문제 삼지 않는다 (문장 재배열은 정상적인 다듬기다)<br>· 개수나 값이 달라진 것만 잡는다<br>· **날짜는 표기가 달라도 같은 날이면 같다고 본다** — "2026-08-26", "2026년 8월 26일", "26일"을 같은 날짜로 정규화한 뒤 비교한다. 이걸 안 하면 정상적인 표기 통일이 전부 경고로 뜬다<br>· 숫자와 날짜를 **나눠서 집계**한다(`counts`). 어느 쪽 문제인지 알아야 사용자가 확인할 지점을 좁힐 수 있다 |
| **수행 내용** | 1. MCP `fact_issues` 호출<br>2. 정규식으로 숫자·날짜 후보 추출 → 날짜 정규화 → 다중집합 비교<br>3. 차이가 나면 `issues`에 고정 안내 문구로 기록<br>4. 화면용 `text` 앞에 경고로 붙인다. **문서 내용(어떤 숫자였는지)은 로그에 남기지 않는다** |
| **사용 tool / Function** | **MCP 도구**: `genon_text_guard.fact_issues`<br>**라이브러리**: `re`(수치·날짜 패턴), `collections.Counter`(다중집합 비교), `datetime`(날짜 정규화)<br>**LLM 모델**: 사용하지 않음 |

---

### S2-05 · 변경 내역 산출

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | "무엇을 어떻게 고쳤는지" 목록을 만들어 답변 끝에 붙인다. **AI에게 물어보지 않고** 프로그램이 원문과 결과를 직접 비교해서 만든다 |
| **목적** | 사용자가 어떤 표현이 어떻게 바뀌었는지 한눈에 확인하고, 원치 않는 변경을 되돌릴 수 있게 한다 |
| **트리거 조건** | S2-03·S2-04와 동시. LLM 응답 직후 자동 실행 |
| **사용자 질의 예시** | 직접 요청하지 않는다. 결과는 답변 하단에 이렇게 표시된다:<br>`---`<br>`**주요 변경 내역**`<br>`- \`하였음\` → \`하였습니다\``<br>`- \`추진코자 함\` → \`추진하고자 합니다\`` |
| **입력 조건** | · `source`(원문), `revised`(결과)<br>· `max_items` — 표시할 최대 건수. 1~500 사이, **기본 50건**. 상한을 넘으면 앞쪽 50건만 낸다 |
| **분석·판단 기준** | · 줄 단위로 먼저 나누고, 그 안에서 문장 단위로 다시 나눠 대조한다<br>· **실제로 달라진 쌍만** 남긴다. 그대로인 문장은 목록에 넣지 않는다<br>· `before`와 `after`가 **둘 다 있는 항목만** 표시한다. 한쪽만 있는 건(순수 추가·순수 삭제) 목록에서 뺀다 — 문맥 없이 보면 오히려 혼란스럽기 때문<br>· 남는 항목이 없으면 변경 내역 블록 자체를 붙이지 않는다 |
| **수행 내용** | 1. MCP `diff_changes` 호출 (원문·결과·최대 건수)<br>2. `difflib`으로 문장 단위 차이 계산<br>3. `_format_changes()`가 마크다운 목록으로 조립 — 구분선 + 헤더 + `` `이전` → `이후` `` 항목<br>4. **화면용 `text`에만** 붙인다. 다운로드용 `polished_text`에는 붙이지 않는다 |
| **사용 tool / Function** | **MCP 도구**: `genon_text_guard.diff_changes`<br>**라이브러리**: `difflib`(파이썬 표준 라이브러리의 텍스트 비교 모듈 — `SequenceMatcher`)<br>**LLM 모델**: 사용하지 않음 |

> **왜 AI에게 안 묻는가**: "뭘 고쳤는지 요약해줘"라고 되물으면 모델이 실제 변경과 다른 요약을 지어낼 수 있다. 그리고 그 요약이 맞는지 검증할 방법이 없다. `difflib`은 항상 실제 차이만 낸다.

---

## 나. SFR-018 번역

### S3-01 · 번역 방향 검증

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | "영어를 러시아어로 번역해줘"는 이 시스템이 지원하지 않는다. **번역을 시작하기 전에 미리 거절**하는 단계다. 30초를 기다린 끝에 실패하는 것보다 낫다 |
| **목적** | 지원 범위 밖의 번역 요청을 사전에 차단하고, 재시도가 무의미하다는 사실을 명확히 전달한다 |
| **트리거 조건** | 번역 캔버스 실행 직후, 첫 스텝(`sfr018_translate_01_detect`)에서 원본을 확보한 다음 |
| **사용자 질의 예시** | · "이 계약서 영어로 번역해줘" → 허용 (한국어 → 영어)<br>· "이 영문 매뉴얼 한국어로" → 허용 (영어 → 한국어)<br>· "이 영어 문서를 러시아어로" → **거부** (한국어가 없다)<br>· "이거 번역해줘" (대상 언어 미지정) → 캔버스 드롭다운 기본값 사용 |
| **입력 조건** | · 원본 텍스트 — hwpx 경로(`translate_hwpx_path`), 업로드 파일, 채팅 입력 중 하나<br>· 대상 언어(`translate_target_lang`) — 필수<br>· 원본 언어(`translate_source_lang`) — **비워도 된다.** 비우면 텍스트 표본으로 자동 감지<br>· 문체(`translate_register`) — 문어체/구어체<br>· 상한: 노드 2,000개, 전체 500,000자, 업로드 20MB |
| **분석·판단 기준** | ① **문자 체계로 언어를 감지**한다. 한글 자모 범위, 한자, 키릴 문자, 타이 문자, 라틴 문자의 출현 비율로 판정한다. AI를 쓰지 않으므로 항상 같은 답이 나온다<br>② 감지 실패는 **오류가 아니다.** 빈 문자열을 돌려주고, 사용자가 지정한 원본 언어를 그대로 쓴다<br>③ **원본과 대상 중 하나는 반드시 한국어**여야 한다. 한국어를 축으로 하는 사내 번역 용도이고, 비한국어 쌍은 용어사전도 검증도 성립하지 않는다<br>④ 지원 언어 6종: 한국어·영어·중국어·태국어·베트남어·러시아어<br>⑤ 사용자가 지정한 원본 언어와 감지 결과가 다르면 거부하지 않고 **`translate_source_mismatch`로 표시만** 한다 (사용자 지정이 맞을 수도 있다) |
| **수행 내용** | 1. hwpx 경로가 있으면 MCP `hwpx_to_markdown`으로 **직접 파싱** (전처리기를 거치면 표 안 수치가 깨진다)<br>2. MCP `validate_direction` 호출<br>3. `allowed=false`면 **고정 안내문을 내고 종료**. `retryable=false`로 표시해 "잠시 후 다시 시도" 안내가 뜨지 않게 한다<br>4. `allowed=true`면 확정된 언어 쌍과 원본을 다음 스텝에 전달<br>5. MCP `resolve_register`로 문체 값 정규화 |
| **사용 tool / Function** | **MCP 도구**: `genon_lang_policy.detect_language`, `genon_lang_policy.validate_direction`, `genon_lang_policy.resolve_register`, `genon_hwpx_text.hwpx_to_markdown`<br>**라이브러리**: `httpx`, `unicodedata`(문자 체계 판별), `re`, `base64`(hwpx 바이트 전달), `lxml`(hwpx 내부 XML 파싱 — MCP 쪽)<br>**LLM 모델**: 사용하지 않음 |

> **왜 "재시도 무의미"를 명시하는가**: 상태코드만 보고 판단하면 이 거부가 일시적 오류로 분류된다. 그러면 사용자는 **몇 번을 눌러도 같은 자리에서 실패하는 문제에 "잠시 후 다시 시도해 주세요"를 반복해서 보게 된다.** 그래서 응답 본문의 `error_code`(`00020003`)로 판정한다.

---

### S3-02 · 스켈레톤 분해와 재조립 (구조 보존)

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | 표가 있는 문서를 AI에게 통째로 번역시키면 **표가 무너진다.** 그래서 표의 뼈대는 프로그램이 따로 보관해 두고, **칸 안의 글자만** AI에게 보낸 뒤, 번역된 글자를 원래 뼈대에 다시 끼워 넣는다 |
| **목적** | 번역 결과의 문서 구조가 원본과 **완전히 동일**하도록 보장한다. AI가 무엇을 출력하든 구조는 흔들리지 않는다 |
| **트리거 조건** | `POST /translate/markdown` 또는 `POST /translate/hwpx` 요청이 코드서빙에 도착한 직후, LLM 호출 **전** |
| **사용자 질의 예시** | · "이 재무제표 영어로 번역해줘" (표가 20개인 문서)<br>· "회의록 번역해줘" (제목·목록·표가 섞인 문서)<br>· "코드 예제가 들어간 기술문서 번역" (코드블록은 번역하면 안 된다) |
| **입력 조건** | · 마크다운 또는 HTML 형태의 원본<br>· hwpx인 경우 MCP가 이미 마크다운/HTML로 바꿔 놓은 상태<br>· 배치 단위: 한 번에 최대 10개 항목, 4,000자 (`MAX_ITEMS_PER_BATCH`, `MAX_CHARS_PER_BATCH`)<br>· 동시 호출 15개 (`LLM_CONCURRENCY`) |
| **분석·판단 기준** | 코드가 다음을 **번역 대상이 아닌 골격**으로 분리한다:<br>· 표 파이프(`\|`)와 구분선 행<br>· HTML 태그 (`<table>`, `<tr>`, `<td>` 등)<br>· 제목 마커(`#`), 목록 마커(`-`, `1.`)<br>· 코드블록(``` 로 감싼 부분) — **안의 내용도 번역하지 않는다**<br><br>**지켜야 할 계약 2가지**<br>① **무손실** — 원본과 같은 언어로 번역(항등 번역)하면 결과물이 입력과 **문자 하나까지 동일**해야 한다<br>② **구조 불변** — 번역 후에도 줄마다 파이프 개수와 마커가 원본과 같아야 한다 |
| **수행 내용** | 1. **분해** — 원본을 골격과 번역 대상 텍스트 조각(unit)으로 나눈다<br>2. **중복 제거** — 같은 원문은 한 번만 LLM에 보낸다(`DEDUPE_UNITS`). 표의 반복되는 머리글("구분", "합계")이 매번 다르게 번역되는 것을 막고 비용도 줄인다<br>3. **빈 호출 회피** — 번역할 텍스트가 하나도 없는 문서(순수 숫자 표)는 **LLM을 아예 부르지 않는다**<br>4. **번역** — S3-03<br>5. **재조립** — 번역 결과를 원래 자리에 끼워 넣는다<br>6. **후처리 2가지**<br>  · 번역문 안의 파이프 문자를 이스케이프(`\|`). 안 하면 **그 행부터 열이 전부 밀린다**<br>  · 번역문의 줄바꿈을 제거. **줄바꿈 하나에 표 행이 두 줄로 갈라진다**<br>7. **폴백** — 조각 개수가 안 맞아 재조립에 실패하면 그 부분은 원문을 유지하고 `fallback_rate`에 집계한다 |
| **사용 tool / Function** | **자체 모듈**: `markdown_units.py`(분해·재조립), `translator.py`(배치 관리)<br>**라이브러리**: `re`(골격 패턴 인식), `asyncio`(동시 배치 호출 + `Semaphore`로 15개 제한), `httpx`, `lxml`(HTML 표 처리)<br>**LLM 모델**: S3-03에서 호출. 이 단계 자체는 코드만 돈다<br>**검증**: `SFR-018/tests/test_markdown_units.py` — 항등 번역 무손실과 구조 불변을 자동 확인 |

---

### S3-03 · 문장 번역 (본 작업)

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | 뼈대에서 떼어낸 **글자 조각들만** AI에게 보내 번역시키는 단계다. 한 번에 10개씩 묶어 보내고, 15묶음까지 동시에 처리한다 |
| **목적** | 분리된 텍스트 조각을 대상 언어로 변환한다. 문체(문어체/구어체)와 사내 용어사전을 함께 지시한다 |
| **트리거 조건** | S3-02의 분해가 끝나고 번역 대상 조각이 1개 이상일 때 |
| **사용자 질의 예시** | (S3-01·S3-02와 동일한 요청의 연속. 사용자가 이 단계를 따로 부르지 않는다) |
| **입력 조건** | · 텍스트 조각 배열 (배치당 최대 10개 / 4,000자)<br>· 확정된 원본·대상 언어, 문체<br>· 해당 배치에 걸린 용어사전 항목 (S3-04)<br>· 응답 상한 16,384 토큰 (`MAX_TOKENS`) |
| **분석·판단 기준** | · **모델 온도 0.3** — 번역은 의역 여지가 필요하지만 창작해서는 안 된다<br>· 번역 단위의 프롬프트만 **전부 영어**로 쓴다. 다른 단위는 한국어 지시가 섞여 있는데, 번역은 지시문에 한국어가 있으면 모델이 그 한국어까지 번역 대상으로 오인한다<br>· 조각별로 번역하되, **같은 배치 안의 조각들은 서로 문맥이 된다**<br>· 재시도 2회. 연결 3초 / 읽기 90초로 나눠 잡는다 |
| **수행 내용** | 1. 배치 구성 (10개 / 4,000자 상한 중 먼저 걸리는 쪽)<br>2. 프롬프트 렌더링 — 언어 쌍, 문체, 용어사전 지시 삽입<br>3. `asyncio.Semaphore(15)`로 동시 호출 수를 묶어 GenOS 게이트웨이로 전송<br>4. 실패한 배치만 재시도. **성공한 배치는 다시 부르지 않는다**<br>5. 결과 조각을 S3-02의 재조립에 넘긴다 |
| **사용 tool / Function** | **LLM 모델**: `LLM_MODEL_ID` 환경변수로 지정된 폐쇄망 반입 모델<br>**호출 경로**: GenOS Gateway OpenAI 호환 `/chat/completions`<br>**라이브러리**: `httpx.AsyncClient`, `asyncio`(`gather` + `Semaphore`), `jinja2`(프롬프트 템플릿)<br>**파라미터**: temperature 0.3, max_tokens 16384, stream false, 동시 15, 재시도 2 |

---

### S3-04 · 사내 용어사전 적용과 준수율 재계산

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | "제논"을 "Genon"으로, "기안문"을 "Draft Document"로 — 회사가 정해 둔 번역어를 지키게 하는 단계다. **지시만 하고 끝내지 않고, 정말 지켰는지 프로그램이 다시 세어본다** |
| **목적** | 사내 표준 용어의 번역 일관성을 확보하고, 실제 준수 여부를 사용자에게 수치로 보고한다 |
| **트리거 조건** | S3-03 호출 **전**(사전 조회)과 **후**(준수율 계산), 두 번 |
| **사용자 질의 예시** | · "우리 회사 용어 기준으로 영문 번역해줘"<br>· (별도 요청 없이도 항상 자동 적용된다) |
| **입력 조건** | · 번역할 문장 목록 — **한 번에 최대 200개**<br>· 대상 언어 코드<br>· 용어사전 출처 3종이 **모두** 설정돼 있어야 한다: `TRANSLATE_GLOSSARY_API_URL`, `TRANSLATE_GLOSSARY_DRIVE_ID`, `TRANSLATE_GLOSSARY_WORKSPACE_ID` |
| **분석·판단 기준** | **조회 단계**<br>· 문장에서 용어 후보를 뽑되 **최대 6단어**까지만 본다(`_MAX_TERM_WORDS`). 더 길면 용어가 아니라 문장이다<br>· 같은 용어가 여러 문장에 나와도 **한 번만** 낸다<br><br>**재계산 단계**<br>· 번역 결과에서 지정 번역어가 실제로 나타나는지 **문자 단위로 대조**한다<br>· 준수율 = (지켜진 용어 수) / (걸린 용어 수)<br>· 프롬프트에 넣었다고 지켜졌다고 가정하지 않는다<br><br>**폴백이 없다**<br>· 사전을 못 가져오거나 캐시 상한(30만 건)을 넘으면 **그 언어는 용어사전 없이 번역된다**<br>· 조용히 넘어가지 않고 `enabled=false`와 `reason`, `glossary.source`로 반드시 드러난다 |
| **수행 내용** | 1. MCP `glossary_lookup` — 문장 목록과 대상 언어로 걸린 용어 조회<br>2. `enabled` 값 확인. `false`면 그 사실을 응답에 싣는다<br>3. 용어 지시를 프롬프트에 삽입해 S3-03 실행<br>4. 번역 결과를 받아 `glossary_report.build_report()`로 **준수율 재계산**<br>5. `glossary` 필드로 사용자에게 보고 (걸린 용어 수, 지켜진 수, 출처) |
| **사용 tool / Function** | **MCP 도구**: `genon_glossary.glossary_lookup`, `genon_glossary.glossary_status`, `genon_glossary.glossary_reload`<br>**자체 모듈**: `glossary_report.py`(준수율 재계산)<br>**외부 API**: GenOS AI 드라이브 용어사전 API (admin-api). 2026-08-14부터 볼륨 파일이 아니라 이 API에서 받는다 — 관리 화면에서 등록한 용어가 **볼륨에 파일을 올릴 필요 없이 곧바로 반영**된다<br>**라이브러리**: `httpx`, `re`(용어 경계 인식), `functools.lru_cache`(사전 캐시)<br>**LLM 모델**: 조회·재계산 단계에서는 사용하지 않음 |

> **한계**: 태국어·중국어는 띄어쓰기가 없어 단어 경계를 잡기 어렵다. 토큰이 길게 잡히므로 **사실상 완전 일치하는 용어만** 걸린다.
> **`glossary_reload`가 호스트를 인자로 받지 않는 이유**: 도구 인자로 받으면 MCP를 통해 **임의의 외부 호스트를 호출**할 수 있게 된다. 환경변수로 고정된 드라이브만 다시 읽는다.

---

### S3-05 · 숫자 보존 확인

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | 번역하면서 숫자가 바뀌지 않았는지 확인한다. **"1,000"이 "1000"이 된 건 괜찮고, "100"이 된 건 문제다** |
| **목적** | 번역 결과의 수치 정합성을 확인한다. 계약서·재무 문서에서 숫자 변조는 치명적이다 |
| **트리거 조건** | 번역이 끝나고 마지막 스텝(`sfr018_translate_02_translate`)이 결과를 화면에 내기 직전 |
| **사용자 질의 예시** | 직접 요청하지 않는다. 결과는 "⚠ 원문의 수치 2건이 번역문에서 확인되지 않습니다"로 표시된다 |
| **입력 조건** | · `source`(원문), `revised`(번역 결과) |
| **분석·판단 기준** | · **자릿수 구분 기호를 떼고 비교**한다. 언어마다 천 단위 표기가 다르다 — 한국어 `1,000`, 러시아어 `1 000`, 독일식 `1.000`. 이걸 정규화하지 않으면 정상 번역이 전부 경고로 뜬다<br>· 다중집합 대조 — 순서는 문제 삼지 않고 개수·값만 본다<br>· 정책 2가지 (`TRANSLATE_NUMERIC_GUARD`)<br>  · `warn`(기본) — 경고만 낸다<br>  · `revert` — 수치가 어긋난 조각은 **원문을 그대로 유지**한다 |
| **수행 내용** | 1. MCP `numeric_issues` 호출<br>2. `has_drift`가 참이면 `drift` 상세와 함께 경고 생성<br>3. 정책이 `revert`면 해당 조각을 원문으로 되돌린다<br>4. 화면용 `text`에 경고를 붙인다<br>5. **`drift` 값은 로그에 싣지 않는다** — 문서 내용이기 때문이다 (로그 화이트리스트 규약) |
| **사용 tool / Function** | **MCP 도구**: `genon_text_guard.numeric_issues`<br>**라이브러리**: `re`, `collections.Counter`, `unicodedata`(전각 숫자 정규화)<br>**LLM 모델**: 사용하지 않음 |

---

## 다. SFR-018 FAQ 생성

### S4-01 · 원본 확보와 생성 개수 결정

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | "FAQ 20개 뽑아줘"라고 해도 회사가 정한 상한이 10개면 10개만 만든다. **그리고 깎았다는 사실을 반드시 알려준다** |
| **목적** | 문서를 확보하고, 관리자가 정한 예산 안에서 실제 생성 개수를 확정한다 |
| **트리거 조건** | FAQ 캔버스 실행 직후, 첫 스텝(`sfr018_faq_01_source`) |
| **사용자 질의 예시** | · "이 사규에서 FAQ 10개 뽑아줘"<br>· "자주 물어볼 만한 질문 만들어줘" (개수 미지정 → 기본 5개)<br>· "FAQ 50개" (상한 10개면 → 10개로 깎고 그 사실을 표시) |
| **입력 조건** | · 원본 — hwpx 경로(`faq_hwpx_path`), 업로드 파일, 채팅 입력 중 하나<br>· 요청 개수(`faq_count`) — 0~1000. 0이면 기본값<br>· 캔버스 상한(`faq_max_count`) — 선택<br>· 제목(`faq_title`) — 파일명·헤더에 쓴다<br>· 컨텍스트 상한 24,000자 (`FAQ_MAX_CONTEXT_CHARS`), 업로드 20MB |
| **분석·판단 기준** | **개수는 세 값의 교집합**이다:<br>배포 상한(`FAQ_MAX_COUNT`, 기본 10) ∩ 캔버스 상한(`faq_max_count`) ∩ 사용자 요청<br>· 캔버스 상한은 **배포 상한 안에서만 낮출 수 있다.** 캔버스가 배포 상한을 넘길 수 있으면 관리자가 정한 LLM 예산이 설정 하나로 무력해진다<br>· 깎였으면 `faq_effective_max`로 노출한다<br><br>**길이 초과 처리가 글다듬이와 다르다**<br>· FAQ는 **앞부분만 쓰고 `source_truncated`로 알린다**<br>· 글다듬이는 **거절한다**<br>· 차이의 이유: FAQ는 "문서에서 뽑기"라 부분 입력에도 결과가 성립한다. 되쓰기는 뒷부분이 사라진 결과가 정상 응답처럼 나가므로 성립하지 않는다 |
| **수행 내용** | 1. hwpx 경로가 있으면 MCP `hwpx_to_markdown`으로 직접 파싱 (우선)<br>2. 없으면 업로드 파일 또는 전처리기 산출 마크다운 사용<br>3. `GET /config`로 배포 상한·기본 개수 확인<br>4. 세 값의 교집합으로 개수 확정<br>5. 컨텍스트 상한 초과 시 앞부분만 취하고 `source_truncated` 표시<br>6. 세션 ID 확보 (다운로드 때 같은 결과를 내려주기 위해) |
| **사용 tool / Function** | **MCP 도구**: `genon_hwpx_text.hwpx_to_markdown`<br>**코드서빙 API**: `GET /config`<br>**라이브러리**: `httpx`, `base64`(hwpx 전달), `lxml`(hwpx XML 파싱 — MCP 쪽)<br>**세션 저장소**: GenOS 제공 Redis. 워크플로우 pod(생성)와 코드서빙 pod(다운로드)가 **같은 Redis를 본다**<br>**LLM 모델**: 사용하지 않음 |

---

### S4-02 · FAQ 문답 생성

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | AI가 문서를 읽고 "질문 – 답변 – **그 답변의 근거가 된 원문 문장**" 세 쌍을 만든다. 근거를 함께 내게 하는 것이 핵심이다 |
| **목적** | 문서 내용에서 자주 물을 만한 질문과 답변을 추출한다. 각 항목에 **출처 문장**을 붙인다 |
| **트리거 조건** | S4-01이 오류 없이 끝나고 두 번째 스텝(`sfr018_faq_02_generate`)이 시작될 때 |
| **사용자 질의 예시** | · "이 취업규칙에서 직원들이 자주 물을 만한 질문 뽑아줘"<br>· "제품 매뉴얼 기반 FAQ 만들어줘"<br>· "이 공고문 내용으로 Q&A 5개" |
| **입력 조건** | · `faq_source_text` — 확보된 본문 (최대 24,000자)<br>· `faq_count` — 확정된 개수<br>· `faq_session_id` — 결과 저장 키<br>· 응답 대기 90초, 재시도 2회 |
| **분석·판단 기준** | · **모델 온도 0.2** — 세 단위 중 가장 낮다. FAQ는 문서에서 **뽑는** 작업이지 만드는 작업이 아니다. 온도를 올리면 근거 검증 기각률이 곧바로 올라간다<br>· 출력을 **JSON으로만** 내도록 지시한다. 마크다운 백틱이나 설명 문장을 붙이지 않게 한다<br>· 각 항목은 `question`, `answer`, `evidence` 세 필드를 갖는다<br>· `evidence`는 **원문에 있는 문장을 그대로** 적도록 지시한다 (요약하지 말 것) |
| **수행 내용** | 1. 프롬프트 템플릿(`onprem/prompt/SFR-018_faq/*.j2`)에 본문·개수를 렌더링<br>2. GenOS 게이트웨이로 LLM 호출<br>3. 응답에서 ```` ```json ````과 백틱을 제거하고 JSON 파싱<br>4. 파싱 실패 시 재시도. 2회 실패하면 오류 반환<br>5. 파싱된 항목을 **근거 검증(S4-03)에 넘긴다** |
| **사용 tool / Function** | **LLM 모델**: `LLM_MODEL_ID` 환경변수로 지정된 폐쇄망 반입 모델<br>**호출 경로**: GenOS Gateway OpenAI 호환 `/chat/completions`<br>**라이브러리**: `httpx.AsyncClient`, `json`(응답 파싱), `jinja2`(프롬프트), `fastapi`+`pydantic`(스키마 검증)<br>**파라미터**: temperature 0.2, stream false, timeout(connect 3s / read 90s), 재시도 2 |

---

### S4-03 · 근거 정합성 검증

| 항목 | 내용 |
|---|---|
| **쉽게 말하면** | AI가 "이 답변은 문서 3쪽에 근거가 있습니다"라고 말했을 때, **정말 그 문장이 문서에 있는지 프로그램이 직접 찾아본다.** 없으면 그 FAQ는 버린다 |
| **목적** | "문서에서 뽑았다"는 계약을 실제로 보증한다. 지어낸 답변에 그럴듯한 출처가 붙는 상황을 막는다 |
| **트리거 조건** | S4-02의 응답을 파싱한 직후, 사용자에게 보여주기 **전**. 모든 항목에 대해 자동 실행 |
| **사용자 질의 예시** | 직접 요청하지 않는다. 결과는 이렇게 표시된다:<br>"FAQ 5건을 생성했습니다. (근거 미확인 2건, 중복 1건 제외)" |
| **입력 조건** | · LLM이 제시한 `evidence` 문장<br>· 원천 문서 본문<br>· 임계값 `FAQ_EVIDENCE_MIN_RATIO` — **기본 0.8** (1.0으로 두면 완전 일치만 인정)<br>· 처리 정책 `FAQ_EVIDENCE_REJECT` — **기본 1(버림)**. 0으로 두면 경고만 달고 남긴다 |
| **분석·판단 기준** | **2단계 판정**<br>① 근거 문장이 원문에 **완전히 포함**되면 점수 1.0 → 즉시 채택<br>② 아니면 **3-gram(연속 3글자) 겹침 비율**을 계산해 임계값(0.8)과 비교<br>  · 정규화 후 문자 단위 포함률이다<br>  · 띄어쓰기·문장부호 차이로 완전 일치가 깨지는 경우를 구제하기 위한 장치다<br><br>**기각 사유 3종을 나눠 집계한다**<br>· `schema` — 필드가 빠졌거나 형식이 잘못됨<br>· `ungrounded` — 근거를 원문에서 확인할 수 없음<br>· `duplicate` — 이미 나온 질문과 중복<br><br>**주의**: 어휘 중복이 낮다고 곧장 오답은 아니다. AI가 원문을 재서술했을 수 있다. 그래서 임계 미달 건에만 확인 표시를 붙이는 정책(`REJECT=0`)도 선택 가능하다 |
| **수행 내용** | 1. `evidence.py`가 각 항목의 근거를 원문과 대조<br>2. 완전 포함 → 채택 / 3-gram 비율 ≥ 0.8 → 채택 / 미달 → 기각<br>3. 채택된 항목끼리 질문 중복 검사<br>4. 기각 건수를 사유별로 `stats`에 집계<br>5. **기각 건수를 전부 응답에 노출**한다. 조용히 버리면 5개 요청에 3개만 나온 이유를 사용자가 알 수 없다<br>6. 채택 항목을 Redis 세션에 저장 |
| **사용 tool / Function** | **자체 모듈**: `faq/evidence.py`(근거 대조), `faq/generator.py`(파이프라인)<br>**라이브러리**: `re`(정규화), `difflib`·n-gram 집합 연산(겹침 비율), `unicodedata`(전각·반각 통일), `redis`(결과 저장)<br>**LLM 모델**: 사용하지 않음 — **판정은 코드가 한다** |

> **다운로드는 저장된 것을 내려준다.** `POST /download`가 LLM을 다시 부르면 화면에서 본 FAQ와 파일 내용이 달라진다. 그래서 Redis에 저장된 결과를 그대로 낸다.
> **다운로드가 세션을 지우지 않는다.** SFR-006과 다른 점이다. 사용자가 같은 FAQ를 여러 번 받을 수 있어야 한다.

---

## 라. 세 기능 공통 비교

| 구분 | 글다듬이 | 번역 | FAQ 생성 |
|---|---|---|---|
| LLM에 보내는 단위 | 문서 **전체** | 텍스트 **조각**(배치 10개) | 문서 **전체**(24,000자까지) |
| 구조 보호 방식 | 사후 대조 (S2-03) | 사전 분리 (S3-02) | 해당 없음 |
| 모델 온도 | 0.3 | 0.3 | **0.2** |
| 길이 초과 시 | **거절** | 상한 검사 후 거절 | **앞부분만 사용 + 표시** |
| 상태 저장 | **없음** | 없음 | Redis |
| 검증 실패 시 | 경고만, 결과는 전달 | 정책에 따라 경고 또는 원문 유지 | **기각(기본)** 또는 경고 |
| 산출물 | txt | txt | txt |

**세 기능 모두 공통으로 쓰는 것**

| 구분 | 내용 |
|---|---|
| LLM 호출 통로 | GenOS Gateway OpenAI 호환 `/chat/completions` 하나뿐 |
| 모델 지정 | `LLM_MODEL_ID` 환경변수. 코드에 모델명을 적지 않는다 |
| 인증 | `GENOS_TOKEN` — 코드에 기본값을 두지 않고 **호출 시점에** 검증한다 |
| 타임아웃 | connect 3초 / read 90초 / write 5초 — 연결은 빨리 포기하고 생성은 길게 기다린다 |
| 재시도 | 2회 (`LLM_RETRY_COUNT`) |
| 웹 프레임워크 | `fastapi` + `pydantic`(요청·응답 스키마), `uvicorn`(서버) |
| HTTP 클라이언트 | `httpx.AsyncClient`. **호출마다 열고 닫는다** (전역 커넥션 금지) |
| 프롬프트 | `jinja2` 템플릿. `onprem/prompt/<단위명>/*.j2`에 두고 이미지에 함께 넣는다. 없으면 빈 프롬프트로 넘어가지 않고 **요청을 세운다** |
| hwpx 파싱 | `lxml` + `zipfile` (hwpx는 XML을 담은 zip이다) |
| 산출 파일 | `txt_output.py` — **UTF-8 BOM + CRLF**. 옛 윈도우 메모장이 BOM 없는 UTF-8을 cp949로 읽어 한글을 깨뜨리고, LF만 있는 파일을 한 줄로 붙여 보여주기 때문 |

