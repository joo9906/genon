{#
  부족분 재요청 — 마크다운 형식. 이미 채택된 질문 목록을 주고 **다른** 질문을 받는다.

  변수
    document       : 그 조각 본문
    missing        : 더 필요한 개수
    existing_block : "- 질문" 줄들 (코드가 조립한다 — 로더에 `{% for %}` 가 없다)
#}
문서 내용:
{{ document }}

이미 만든 질문입니다. 이것들과 **겹치지 않는** 질문을 {{ missing }}개 더 만드세요.
{{ existing_block }}

위 형식(`<<<FAQ` … `>>>`)을 그대로 지켜서 출력하세요.
