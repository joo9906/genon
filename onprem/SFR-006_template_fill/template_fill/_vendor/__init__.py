"""외부 코드의 **벤더 사본**. 우리가 쓴 코드가 아니다 — `README.md` 를 먼저 읽을 것.

여기 들어오는 조건은 하나다: 폐쇄망 registry 에 wheel 이 있으리라 보장할 수 없는데,
그 코드가 우리에게 없는 판정·측정 능력을 주고, 우리가 다시 만들면 더 나빠지는 경우.
지금은 `hwpx`(python-hwpx, Apache-2.0) 하나뿐이다.

이 디렉토리 아래 파일은 **직접 고치지 않는다.** 동작을 바꾸고 싶으면 상류에 반영하거나,
우리 모듈(`overflow.py`·`hwpx_verify.py`)의 어댑터 층에서 감싼다 — 여기를 고치면
재동기화 때 조용히 되돌아간다.
"""
