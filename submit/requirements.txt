# genon — `onprem/` 과 `not/` 을 돌리기 위한 라이브러리 모음집
#
# **이 파일은 배포 단위가 설치하는 파일이 아니다.** GenOS 코드 서빙은 단위마다 자기
# `requirements.txt` 를 설치한다(가이드 6.3) — 정본은 아래 여덟 파일이고, 이 파일은
# 그 합집합 + 배포 단위가 아닌 것들(eval·전처리기·점검 스크립트)까지 모은 **개발·
# 로컬 검증용** 목록이다. 단위 목록을 고칠 때 여기도 같이 고칠 것.
#
#   onprem/codeserving/SFR-006_template_fill/requirements.txt
#   onprem/codeserving/SFR-018_text_polish/requirements.txt
#   onprem/codeserving/SFR-018_translation/requirements.txt
#   onprem/codeserving/SFR-018_faq/requirements.txt
#   not/SFR-006_template_fill/requirements.txt        (lxml 없는 한시 판본)
#   not/SFR-018_text_polish/requirements.txt
#   not/SFR-018_translation/requirements.txt
#   not/SFR-018_faq/requirements.txt
#   onprem/eval/requirements.txt
#
# 버전은 단위 파일과 **같은 하한**으로 맞췄다. 하한을 여기서만 올리면 로컬은 되는데
# 배포는 옛 버전으로 도는 상태가 되고, 그 어긋남은 오류로 드러나지 않는다.
#
# 폐쇄망이면 사내 PyPI mirror 에 있는지 먼저 확인한다(가이드 11.5.6). `lxml` 이 없어
# 막힌 것이 `not/` 이 생긴 이유다.


# ── 코드 서빙 4단위 공통 (onprem/codeserving, not/) ──────────────────────
fastapi>=0.110
uvicorn>=0.30
pydantic>=2.6
# 게이트웨이 경유 LLM·MinIO·프롬프트 라이브러리 호출. 워크플로우 스텝 9개가 쓰는
# 유일한 외부 패키지이기도 하다(스텝은 pod 기본 이미지로 돈다 — 이 파일을 설치하지 않는다).
httpx>=0.27

# multipart 폼 파싱. `File(...)`/`Form(...)` 라우트를 등록하는 순간 필요하므로
# 없으면 런타임이 아니라 **기동 단계에서** 죽는다.
#   onprem: 006(`POST /templates`·`/generate/upload`) · 번역(`/translate/hwpx`) · FAQ(`/faq/hwpx`)
#   not/  : 006 만 (번역·FAQ 는 업로드 라우트를 뺐다)
python-multipart>=0.0.9

# 멀티턴 세션 (워크플로우 pod ↔ 코드 서빙 pod 공유). 006 · FAQ 만 쓴다.
# 글다듬이·번역은 무상태다.
redis>=5.0

# hwpx 읽기/되쓰기. **`not/` 네 단위는 이것을 쓰지 않는다** — 사내 mirror 에 없어
# 표준 `xml.etree` 로 옮긴 것이 그 판본의 요점이다(`not/README.md`).
#   onprem: 006(hwpx_fields·hwpx_blocks·hwpx_style) · 번역·FAQ(hwpx_text) ·
#           eval(structure_metrics) · 전처리기 2벌 · 점검 스크립트
lxml>=5.0


# ── MCP 파일 4개 (onprem/mcp) ────────────────────────────────────────────
# `requirements.txt` 라는 개념이 없는 등록 방식이라(파일 1개 = 등록 단위) 아래는
# **기본 이미지에 있기를 기대하는 것**이다. `pydantic` 은 FastMCP 가 스키마를 만들 때
# 이미 쓰는 패키지이고, 없으면 도구 인자가 맨 `str` 로 떨어져 그냥 돈다(선택 의존).
# 네 파일이 그 밖에 쓰는 것은 표준 라이브러리뿐이다 — 용어사전·프롬프트 조회도
# `httpx` 가 아니라 `urllib` 로 한다.


# ── 평가지표 MCP 서버 (onprem/eval — 배포 단위 아님) ─────────────────────
mcp>=1.2
# ↑ `from mcp.server.fastmcp import FastMCP` 가 정본이고 `fastmcp` 단독 패키지로도
#   폴백한다. 둘 중 하나만 있으면 된다.
# lxml 은 위에 이미 있다.


# ── 전처리기 area 05 (onprem/preprocessor) — 벤더 절반의 의존 ────────────
# `final_preprocessor.py`(첨부용+hwpx) · `smart_preprocessor.py`(지능형+hwpx) ·
# `only_me.py`(첨부용, 파싱 전용).
#
# **hwpx 절반은 `lxml` 하나로 돈다.** 아래는 벤더 절반(PART 1)이 쓰는 것이고 전부
# `try:` 안에 있어, 없으면 그 절반만 비활성으로 드러나고 hwpx 경로는 그대로 돈다.
# **점검 스크립트(`check_final_preprocessor`·`check_smart_preprocessor`)는 벤더
# 스택을 대역으로 꽂으므로 아래를 설치하지 않아도 전부 통과한다** — 실제로 필요한
# 것은 온프레미스 적재 실행뿐이다.
docling
docling-core
langchain-community
langchain-core
langchain-text-splitters
semchunk
transformers
pandas
pypdf
olefile
chardet
pillow
pymupdf
markdown2
pydub
requests
PyYAML
typing_extensions>=4.9
# weasyprint 는 pip 로는 깔리지만 **시스템 라이브러리(pango/cairo)를 요구**한다 —
# 없는 이미지에서는 `pip install -r` 자체가 그 자리에서 실패한다. 첨부용 절반의
# html→pdf 경로 전용이라 필요할 때만 켠다.
# weasyprint


# ── 선택 의존 ────────────────────────────────────────────────────────────
# eval `hwpx_text_crosscheck` 가 lxml 과 **다른 파서**로 문단 보존을 재측정한다.
# 없으면 그 지표만 `available=False`(미측정, 통과 아님)이고 나머지는 그대로 돈다.
python-hwpx>=6.0,<7


# ── pip 로 받을 수 없는 것 (플랫폼이 준다) ───────────────────────────────
# 아래는 사내 mirror 에도 PyPI 에도 없다. 로컬에서는 전부 대역으로 대신하고,
# 실환경에서는 GenOS 이미지가 들고 있다.
#
#   main_socketio      워크플로우(02) 런타임이 주입. 스텝 4개가 토큰을 흘릴 때 쓴다
#   mcp (전역 객체)     MCP 런타임이 주입. 네 파일에 `mcp` 미주입 대비 shim 이 있당
#   genon.preprocessor 벤더 전처리기 패키지 (마스킹·hwp→pdf·enrichment)
#   genos_utils        벤더 미디어 업로드 (`upload_files`)
#   docling 의 GenOS 패치 백엔드
#                      `docling.backend.genos_msword_backend` ·
#                      `docling.backend.genos_hwp_backend` ·
#                      `docling.prompts` · `docling.utils.document_enrichment` 등.
#                      **공개 PyPI 의 `docling` 에는 없다** — 위 `docling` 줄은
#                      공통 API 만 맞춰 주고, 이 심볼들은 사이트 설치본이 낸다.
#                      (2026-09-02 에 `…enrichment.page_description` 가 사이트
#                       설치본에 없어 pdf 적재가 막힌 적이 있다 — PART 1 의 가드 주석)
#
# `not/minio.py` 는 **등록하지 않는다** — GenOS 참조 샘플이고 import 하는 순간
# `pip install python-docx` 를 실행한다(가이드 p.19 금지사항). 그래서 `python-docx`
# 를 여기 넣지 않았다.
