{#
  배치 번역 유저 프롬프트 — 번역 대상 항목만 JSON 으로 넘긴다.

  변수
    items_json : [{"id": <int>, "s": "<원문>"}] 를 json.dumps 한 문자열.
      템플릿에서 조립하지 않고 코드가 만든 문자열을 그대로 싣는다 — jinja 의
      문자열 이스케이프로 JSON 을 만들면 따옴표·역슬래시가 있는 원문에서 깨진다.
#}
{{ items_json }}
