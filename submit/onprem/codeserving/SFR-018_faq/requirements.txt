# SFR-018 FAQ — 코드 서빙(03) 빌드 의존
#
# 가이드 6.3: 빌드 (Build) 커맨드에서 `pip install -r requirements.txt` 를 실행한다.
# 가이드 11.5.6: 폐쇄망이면 이 패키지들이 사내 PyPI registry/mirror 에 있는지 먼저 확인한다.
#
# 이 파일은 **2026-08-11 에야 생겼다.** 이 단위는 2026-08-07 에 배포 단위로 추가됐는데
# 의존성 파일 없이 들어왔고, `check_deploy_contract.py` 의 단위 목록에도 빠져 있어
# 아무도 잡지 못했다. 파일이 없으면 빌드 커맨드가 그 자리에서 실패한다.
#
# 워크플로우(02) 스텝도 비슷한 일을 하지만 이 파일을 설치하지 않는다 — pod 기본 이미지에
# 포함된 패키지만 쓸 수 있고, 없으면 운영팀에 기본 이미지 갱신을 요청해야 한다
# (가이드 11.5.6).
#
# **2026-08-12: 내보내기 의존이 전부 없어졌다.** 산출 형식이 txt 하나로 통일되면서
# openpyxl(xlsx)·weasyprint/markdown(pdf)·genon.preprocessor(hwpx→pdf 변환)가 필요 없다.
# 지금 이 단위가 만드는 파일은 utf-8 문자열이므로 표준 라이브러리로 끝난다.

fastapi>=0.110
uvicorn>=0.30
pydantic>=2.6
# POST /faq/hwpx 업로드의 multipart 폼 파싱. **빠지면 기동조차 못 한다** —
# FastAPI 는 `File(...)`/`Form(...)` 을 쓴 라우트를 등록하는 순간 RuntimeError 를 낸다
# (런타임 실패가 아니라 import 단계 실패다).
python-multipart>=0.0.9
httpx>=0.27
# 프롬프트 렌더(prompt_loader). 없으면 첫 LLM 호출에서 PromptRenderError 로 떨어진다.
jinja2>=3.1
# hwpx **입력** 표 파싱(hwpx_text·hwpx_xml). 모듈 최상단 import 라 없으면 기동 단계에서
# 죽는다. 입력 경로는 이 변경(txt 통일)과 무관하게 그대로다 — 요구사항 §1 은 그대로다.
lxml>=5.0
# 멀티턴 세션 (워크플로우 pod ↔ 코드 서빙 pod 공유). 다운로드가 저장된 FAQ 를 읽는다.
redis>=5.0

# ── 없는 것 ────────────────────────────────────────────────
# openpyxl·weasyprint·markdown 을 쓰지 않는다 (2026-08-12). 산출 형식이 txt 하나가 되면서
# xlsx·pdf 생성 코드를 걷어냈다. weasyprint 는 pip 로 설치되지만 시스템 라이브러리
# (pango/cairo)를 요구해서 **없는 이미지에서는 `pip install -r` 자체가 실패**했고, 그래서
# 예전에도 여기 적지 못한 채 지연 import 로 방어하고 있었다 — 그 분기 자체가 사라졌다.
