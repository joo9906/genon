# 용어사전 사용자 매뉴얼

> ⚠️ **v1.9.2 이후 접근 불가.** v1.9.2부터 AI 드라이브 상세의 **[파일 | 용어사전] 토글이 숨김 처리**되어 용어사전 화면에 진입할 수 없습니다(추후 재노출 예정). v1.9.2 환경에서는 이 문서의 절차를 수행할 수 없으며, v1.9.0~v1.9.1 환경에만 적용됩니다. 대체 기능인 **커스텀 토큰**은 구현 진행 중으로 아직 제공되지 않습니다. 관련: #17636 · #17619
>
> v1.9.2 시점의 AI 드라이브 사용법은 [v1.9.2 AI 드라이브 매뉴얼](../v1.9.2_AI_드라이브/v1.9.2_AI_드라이브.md)을 참고하세요.

## 개요

용어사전은 AI 드라이브 단위로 도메인 전용 용어를 등록·관리해, 문서 처리(임베딩·검색)에서 해당 용어가 하나의 토큰으로 일관되게 다뤄지도록 하는 기능입니다. 관리자는 드라이브 안에 용어명과 설명을 등록하고, 파일로 일괄 가져오거나 내보낼 수 있으며, 변경 이력을 버전으로 관리하고 필요 시 재인덱싱으로 드라이브에 반영합니다.

진입 경로는 **관리자 콘솔 → 데이터 → AI 드라이브 → (드라이브 상세) → 상단 [파일 | 용어사전] 토글 → 용어사전** 입니다. 용어사전 토글은 드라이브 루트에서 관리 권한이 있을 때 노출됩니다.

용어사전의 모든 편집(추가·수정·삭제·업로드·속성 변경)은 곧바로 반영되지 않고 **변경 승인 워크플로**를 거칩니다. 신청하면 승인 대기 상태가 되고, 결재가 완료되어야 실제 용어사전에 적용됩니다.

![AI 드라이브 상세 우측 상단의 파일 · 용어사전 토글 — 용어사전으로 전환한 상태](screenshots/01_overview.png)

| 항목 | 내용 |
|------|------|
| 버전 | v1.9.0 |
| 영역 | 크로스 (admin-front + admin-api) |
| 상태 | 완료 (BE 연동 완료 · stub 잔존 1종: 용어명 중복 검사) |
| 관련 이슈 | #14444(탭·목록) · #14445(추가/수정·영향 범위) · #14446·#14450(가져오기) · #14447(활동·버전 관리) · #14448(변경 신청) · #14449·#14510(재인덱싱) · #14509(승인 시스템) · #14646(운영 이력) · #14650(업로드 형식 검증) · #15013(문구 정리) · #15259(변경 신청 취소 실 API 전환) · #15496(영구 보관 초과 오류 BE 메시지 처리) |
| 최종 확인 | 2026-07-09 · 기준: `admin-front/src/views/data/ai-drive/terminology/*`, `admin-api/src/router/data/glossary_router.py` |

## 주요 개념

| 개념 | 설명 |
|---|---|
| 용어 | 드라이브에 등록하는 단일 항목. 용어명(필수)과 설명(선택)으로 구성됩니다. |
| 인덱싱 상태 | 용어가 드라이브에 실제 반영되었는지 나타내는 상태(동기화됨·재인덱싱 대기·재인덱싱 중·재인덱싱 실패). |
| 재인덱싱 | 아직 반영되지 않은(대기) 용어를 드라이브 임베딩에 실제로 적용하는 작업. |
| 적용 속성(토크나이저 속성) | 용어를 처리할 때 적용할 토크나이저 속성 집합. 드라이브 단위로 설정합니다. |
| 영향 범위 | 특정 용어가 참조·영향을 주는 대상(파일·에이전트)의 목록과 건수. |
| 활동 / 버전 | 용어사전에 가한 변경 기록. 현재 활동(아직 버전으로 굳지 않은 변경)과 스냅샷으로 굳힌 버전 행으로 구성됩니다. |
| 변경 승인(신청) | 용어 추가·수정·삭제·업로드·속성 변경은 즉시 반영되지 않고 승인 결재를 거칩니다. |
| 운영 이력 | 결재가 끝난(승인·반려) 변경 신청의 이력. |

## 용어 목록 보기

용어사전 화면 상단에는 드라이브에 등록된 용어가 테이블로 표시됩니다. 컬럼은 **용어명 · 설명 · 인덱싱 상태 · 수정일** 4종이며, 용어명과 수정일로 정렬할 수 있습니다.

목록 상단의 통합검색에서 검색 범위를 고른 뒤 검색어를 입력하면 서버에서 필터링한 결과가 표시됩니다. 페이지네이션으로 페이지를 이동합니다.

![용어 목록 테이블(용어명·설명·인덱싱 상태·수정일)과 상단 통합검색·속성 설정·활동 및 버전·내보내기·업로드·용어 추가 버튼·사용량(5/2,000) 표기](screenshots/02_term_list.png)

### 인덱싱 상태 배지

각 용어의 반영 상태는 색상 배지로 표시됩니다.

| 상태 | 배지 색 | 라벨 | 의미 |
|------|--------|------|------|
| synced | green | 동기화됨 | 드라이브에 정상 반영된 상태 |
| pending | orange | 재인덱싱 대기 | 변경되었으나 아직 드라이브에 반영되지 않은 상태 |
| reindexing | blue | 재인덱싱 중 | 재인덱싱 작업이 진행 중인 상태 |
| failed | red | 재인덱싱 실패 | 타임아웃·서버 오류로 재인덱싱이 실패한 상태 |

배지는 표시 전용이며 클릭·정렬 동작은 없습니다.

### 용어 등록 한도

드라이브당 용어는 최대 **2,000건**까지 등록할 수 있습니다. 이 한도는 이미 등록된 용어 + 승인 대기 중인 용어 + 업로드로 적재될 유효 건수를 **합산**해 계산합니다.

- 목록 헤더에 현재 사용량이 `현재건수/2000` 형태로 표시됩니다.
- 한도에 도달하면 [용어 추가] 버튼이 비활성화되고 안내 툴팁이 표시됩니다.
- 가져오기(업로드)로 한도를 초과하면 적재가 차단되고 실패 토스트가 표시됩니다.

## 용어 추가·수정

[용어 추가] 버튼을 누르면 용어 추가 모달이 열립니다. 기존 용어 행에서 수정을 선택하면 같은 모달이 수정 모드로 열립니다.

- **용어명**(필수): 최대 30자. 금지 문자(`\ / : * ? " < > |`)를 쓸 수 없고, 공백만 입력할 수 없습니다.
- **설명**(선택): 최대 500자. 값을 비워둘 수 있으나, 공백만 입력할 수는 없습니다.

모달에는 **적용 속성**(text·summary·file_name 등)이 함께 표시됩니다. 적용 속성은 용어마다 지정하는 값이 아니라 [용어사전 > 속성 설정]에서 드라이브 단위로 설정한 값이 자동 적용되는 것으로, 여기서는 안내(읽기 전용)로만 노출됩니다.

[저장] 을 누르면 곧바로 반영되는 것이 아니라 **변경 승인 신청**이 생성됩니다. 신청이 접수되면 "용어 추가(또는 수정) 신청하였습니다." 토스트가 표시되고, 해당 행은 승인 대기 상태가 됩니다.

![용어 추가 모달 — 용어명(최대 30자)·설명(최대 500자) 입력과 적용 속성(text·summary·file_name) 안내](screenshots/03_term_form.png)

### 변경 신청 대기와 신청 취소

승인 대기 중인 용어 행에는 우측 액션에 **[신청취소]** 버튼이 표시됩니다. 신청을 취소하면 대기 표시가 제거됩니다.

## 용어 삭제·일괄 삭제

개별 용어는 행 액션의 삭제에서 확인 다이얼로그를 거쳐 삭제 신청합니다. 여러 용어를 선택해 한 번에 삭제할 때는 일괄 삭제 다이얼로그에서 신청합니다.

일괄 삭제는 선택한 용어들을 **하나의 승인 신청**으로 묶어 처리합니다(건별 개별 호출이 아니라 단일 신청). 삭제 역시 승인 결재가 완료되어야 실제로 반영됩니다.

![용어 여러 개 선택 시 목록 상단에 나타나는 선택 건수(선택 3개)와 [삭제] 액션 바](screenshots/04_bulk_delete.png)

## 가져오기 (파일 업로드)

CSV·XLSX 파일로 용어를 일괄 등록할 수 있습니다. 가져오기 모달에서 파일을 선택하고 [생성] 을 누르면, 먼저 **화면(FE)에서 형식·필수 검증**을 수행합니다.

파일은 **용어명 · 설명 두 칼럼을 헤더로 반드시 포함**해야 합니다. 검증 규칙은 다음과 같습니다.

| 대상 | 오류가 되는 경우 |
|------|------------------|
| 용어명 | 누락 · 30자 초과 · 금지문자(`\ / : * ? " < > \|`) 포함 · 공백만 입력 · 파일 내 용어명 중복 |
| 설명 | 500자 초과 · 공백만 입력 (값 자체는 선택) |
| 헤더 | 용어명·설명 칼럼 누락 · 불필요한 칼럼 존재 · 중복 칼럼 존재 |

검증에서 오류가 1건이라도 있으면 등록이 차단되고 **형식·필수 오류 모달**이 열립니다. 모달에는 입력 규칙 안내, 오류 요약, 유형별 집계, `"{행번호}행 — {사유}"` 형식의 행 단위 오류 목록이 표시되며, 함께 "csv 파일이 유효하지 않습니다." 오류 토스트가 1회 표시됩니다. 파일을 수정한 뒤 다시 업로드하면 됩니다.

오류가 없으면 서버로 전송되어 적재되며, 등록 건수 안내 토스트가 표시됩니다. 드라이브에 이미 존재하는 용어가 파일에 포함되면 서버가 오류로 응답하고, 그 내용이 오류 모달로 매핑되어 표시됩니다(덮어쓰기 없음).

![업로드 모달 — CSV·XLSX 드롭 영역, 선택한 파일, 양식 다운로드(CSV)·생성 버튼](screenshots/05_import_modal.png)

![형식·필수 오류 모달 — 입력 규칙 안내, 오류 요약, "{행번호}행 — {사유}" 행 단위 목록](screenshots/06_import_error_modal.png)

## 내보내기

등록된 용어를 파일로 내보낼 수 있습니다. 내보내기 시, 셀 값이 수식 트리거 문자(`=`, `+`, `-`, `@`, 탭, 캐리지리턴)로 시작하면 앞에 작은따옴표(`'`)를 붙여 텍스트로 강제합니다. 이는 스프레드시트에서 수식으로 실행되는 CSV 인젝션(CWE-1236)을 막기 위한 처리이며, 선행 공백 뒤에 오는 수식(` =cmd`)도 함께 방어합니다.

<!-- 영향 범위 보기 — 07 스크린샷 미확보로 섹션 전체 숨김. 이미지 확보 후 이 주석 블록(<!-- 와 -- >)을 해제하고 07 이미지를 노출한다.
## 영향 범위 보기

영향 범위 패널은 용어가 참조·영향을 주는 대상을 **전체 / 파일 / 에이전트** 탭으로 나눠 각 건수와 목록(페이지당 10건)을 보여줍니다. 용어 추가·수정 모달 양쪽에서 동일하게 표시됩니다.

영향 범위는 **조회 전용**입니다. 목록의 행을 클릭해 이동하는 동작은 없으며, 대상이 없으면 빈 상태가 표시됩니다.

![영향 범위 패널 — 전체/파일/에이전트 탭과 건수 배지, 조회 전용 목록](screenshots/07_impact_scope.png)
-->

## 재인덱싱

용어를 추가·수정·삭제하면 그 변경은 곧바로 드라이브 임베딩에 반영되지 않고 **재인덱싱 대기(pending)** 상태가 됩니다. 대기 용어가 1건이라도 있으면 목록 상단에 재인덱싱 안내 배너와 실행 버튼이 표시됩니다.

- [재인덱싱] 을 실행하면 대기 용어가 일괄로 재인덱싱되며, 진행률이 실시간으로 표시됩니다.
- 진행 상황은 화면을 벗어나도 확인할 수 있도록 **플로팅 위젯**으로 표시되며, 브라우저를 재시작하거나 다시 접속해도 진행 중이던 작업이 복원됩니다.
- 재인덱싱은 **한 번에 한 드라이브만** 진행할 수 있습니다. 다른 드라이브에서 재인덱싱이 진행 중이면 실행 버튼이 비활성화되고 안내 툴팁이 표시됩니다.
- 진행 중인 재인덱싱은 취소할 수 있습니다.
- 타임아웃·서버 오류로 실패하면 해당 용어의 인덱싱 상태가 "재인덱싱 실패"로 표시됩니다.

![목록 상단 재인덱싱 대기 배너 — "5개 용어가 아직 이 드라이브에 반영되지 않았습니다. 재인덱싱이 필요합니다." + 재인덱싱 버튼](screenshots/08_reindex_banner.png)

![재인덱싱 진행률 표시 — "재인덱싱 중 · 벡터 재계산", 진행률(%)·취소 버튼, "진행 중에도 검색은 계속 가능합니다" 안내](screenshots/09_reindex_progress.png)

## 활동 및 버전 관리

활동 및 버전 관리 모달은 드라이브 단위의 변경 이력을 보여줍니다. 목록은 **현재 활동(아직 버전으로 굳지 않은 변경) + 버전 행**으로 구성되며 최신순으로 표시됩니다.

활동 유형은 추가·수정·삭제·업로드·속성 변경 5종으로, 각각 배지로 구분됩니다. 여러 건을 한 번에 올린 업로드는 하나의 [업로드] 항목으로 묶여 표시됩니다.

| 작업 | 설명 |
|------|------|
| 버전 추가 | 현재 활동을 스냅샷으로 굳혀 새 버전 행을 만듭니다. |
| 복원(롤백) | 선택한 버전의 스냅샷으로 용어 전체를 되돌립니다. 복원 후에는 재인덱싱 대기 상태로 전환되어 재인덱싱 배너가 나타납니다. |
| 영구 보관(핀) | 버전을 영구 보관 상태로 고정합니다. 영구 보관은 최대 200개까지 가능합니다. |
| 버전 삭제 | 버전을 삭제합니다. 단, 영구 보관 중인 버전은 보관을 해제해야 삭제할 수 있습니다. |

복원·버전 삭제는 확인 다이얼로그를 거칩니다. 행별 ⋮ 메뉴(복원·핀 등)는 쓰기 권한이 있을 때만 노출됩니다.

![활동 및 버전 관리 모달 — 총 버전 수(영구보관+일반), 최신순 정렬, 버전·변경 사항·수정한 날짜·수정자·보관 컬럼, 버전 추가/삭제 배지, 행 펼침(집계 건수)과 ⋮ 메뉴](screenshots/10_version_modal.png)

## 변경 승인 (신청 → 결재)

용어사전의 편집 5종(추가·수정·삭제·업로드·속성 변경)은 모두 승인 결재를 거칩니다.

- 한 드라이브에 **처리 대기 중인 승인 건이 1건이라도 있으면** 새 신청이 차단됩니다. 이 경우 "해당 용어사전에 처리 대기 중인 승인 건이 있습니다. 먼저 처리 후 신청해 주세요." 안내가 표시됩니다.
- 신청이 정상 접수되면 "승인 신청이 완료되었습니다." 토스트가 표시됩니다.
- 결재(승인/반려)는 승인 담당자가 승인 메뉴에서 처리하며, 검토 모달에서 신청된 용어 정보(용어명·설명·적용 속성·영향 범위)를 확인합니다.

## 운영 이력

용어사전 목록 카드 아래에는 변경 신청의 처리 이력을 보여주는 **운영 이력**이 표시됩니다.

- 통합검색(ID·용어명·승인상태·요청자·승인자)과 페이지네이션을 제공합니다.
- 컬럼은 ID · 구분 · 유형 · 승인 상태 · 요청자 · 요청 일시 · 승인자 · 승인 일시 · 상세 9종입니다. 구분은 신청 대상(용어·적용 속성), 유형은 변경 종류(추가·수정·삭제·업로드·속성변경) 배지로 표시되며, 승인 상태는 배지(승인·반려·취소·대기중 등)로 표시됩니다.
- [상세] 를 누르면 결재 정보(신청 ID·승인 상태·요청자·요청 일시·승인자·승인 일시)와 신청 시점의 용어 정보(용어명·설명·적용 속성)를 담은 상세 모달이 열립니다. 이 용어 정보는 승인 검토 모달과 동일한 화면 조각을 사용합니다.

![용어사전 운영 이력 목록 — ID·구분·유형·승인 상태·요청자·요청 일시·승인자·승인 일시·상세 컬럼과 상태 배지](screenshots/11_history_section.png)

![운영 이력 상세 모달 — 결재 정보(신청 ID·승인 상태·요청자/승인자·일시)와 용어 정보(용어명·설명·적용 속성)](screenshots/12_history_detail.png)

## API 호출 방식

용어사전 API는 admin-api의 `data/ai-drive/{drive_id}/glossary/*` 경로로 제공됩니다. 모든 요청은 인증 토큰과 워크스페이스 헤더(`x-genos-workspace-id`)를 함께 보내야 합니다.

| 메서드 | 경로 (`/data/ai-drive/{drive_id}/glossary` 하위) | 용도 |
|--------|------|------|
| GET | `/terms` | 용어 목록 조회(검색·페이지네이션) |
| POST | `/terms` | 용어 추가 신청 |
| POST | `/terms/{term_id}/update` | 용어 수정 신청 |
| POST | `/terms/{term_id}/delete` | 용어 삭제 신청 |
| POST | `/terms/bulk-delete` | 용어 일괄 삭제 신청(단일 승인 신청) |
| POST | `/terms/import` | 용어 일괄 가져오기 |
| GET | `/terms/{term_id}/impact` | 기존 용어의 영향 범위 조회 |
| GET | `/impact?term=<텍스트>` | 신규 용어 텍스트의 영향도 분석 (⚠️ QA/운영망 Weaviate 암호화로 현재 임시 비활성 · #15315) |
| GET | `/properties` · POST `/properties` | 적용(토크나이저) 속성 조회·저장 |
| POST | `/reindex` · GET `/reindex/status` · POST `/reindex/cancel` | 재인덱싱 시작·상태 조회·취소 |
| GET | `/versions` · POST `/versions` | 버전 목록 조회 · 현재 활동 → 버전 생성 |
| POST | `/versions/{id}/restore` · `/pin` · `/delete` | 버전 복원 · 영구 보관 토글 · 삭제 |
| GET | `/approvals` | 운영 이력(결재 완료 승인 이력) 조회 |
| POST | `/approvals/{approval_id}/cancel` | 대기(pending) 승인 신청 취소 |

```bash
# 용어 목록 조회
curl -X GET "https://{ADMIN_API_HOST}/data/ai-drive/{DRIVE_ID}/glossary/terms?pg=1&pgSize=10" \
  -H "Authorization: Bearer {ACCESS_TOKEN}" \
  -H "x-genos-workspace-id: {WORKSPACE_ID}"

# 용어 추가 신청
curl -X POST "https://{ADMIN_API_HOST}/data/ai-drive/{DRIVE_ID}/glossary/terms" \
  -H "Authorization: Bearer {ACCESS_TOKEN}" \
  -H "x-genos-workspace-id: {WORKSPACE_ID}" \
  -H "Content-Type: application/json" \
  -d '{"term": "용어명", "description": "설명(선택)"}'
```

재인덱싱 진행률은 응답이 아니라 서버센트이벤트(SSE) `reindex-progress` 이벤트로 전달됩니다.

## 오류·예외 처리

| 코드 | 발생 상황 | 사용자에게 표시되는 문구 |
|------|-----------|-------------------------|
| `00030005` | 이미 존재하는 용어명으로 추가·수정 신청 | `이미 등록된 용어입니다: {용어명}` (폼에서는 "이미 등록된 용어입니다") |
| `00030021` | 처리 대기 중인 승인 건이 있는 드라이브에 새 신청 시도 | `해당 용어사전에 처리 대기 중인 승인 건이 있습니다. 먼저 처리 후 신청해 주세요.` |
| — | 용어명 누락 | `용어명을 입력하세요` |
| — | 용어명/설명에 공백만 입력 | `공백만 입력할 수 없습니다` / `내용을 입력하세요` |
| — | 용어명에 금지 문자 포함 | `사용할 수 없는 문자가 포함되어 있습니다` |
| — | 등록 한도(2,000건) 초과 | `드라이브당 용어는 최대 2,000개…` (툴팁·토스트) |
| — | 업로드 파일 형식 오류(칼럼 누락·불필요/중복 칼럼·행 중복) | `파일 형식이 유효하지 않습니다. …` + 형식·필수 오류 모달 + "csv 파일이 유효하지 않습니다." 토스트 |
| — | 업로드 서버 처리 오류 | `업로드 처리 중 오류가 발생했습니다. 파일을 확인 후 다시 시도하세요.` |
| — | 영향 범위 조회 실패 | `영향 범위를 불러오지 못했습니다.` |
| — | 영구 보관 200개 초과 | (BE 응답 메시지로 표시) |
| — | 영구 보관 중인 버전 삭제 시도 | `영구 보관 중인 버전입니다. 보관 해제 후 삭제할 수 있습니다.` |

## 사용자 시나리오

### 새 용어를 하나 추가한다

용어사전 화면에서 [용어 추가] → 용어명·설명 입력 → [신청]. "용어 추가 신청하였습니다." 토스트 후 해당 용어가 승인 대기 상태로 목록에 나타납니다. 승인이 완료되면 재인덱싱 대기 상태가 되고, [재인덱싱] 을 실행해 드라이브에 반영합니다.

### 파일로 여러 용어를 한 번에 올린다

가져오기 모달에서 용어명·설명 칼럼을 갖춘 CSV/XLSX를 선택하고 [생성]. 형식·필수 오류가 있으면 오류 모달의 행 단위 목록을 보고 파일을 고쳐 다시 업로드합니다. 오류가 없으면 등록 건수 안내와 함께 승인 신청이 접수됩니다.

### 잘못 반영한 변경을 되돌린다

활동 및 버전 관리에서 되돌릴 버전을 골라 [복원]. 확인 후 용어 전체가 해당 버전 스냅샷으로 되돌아가고, 재인덱싱 대기 상태로 전환됩니다. 재인덱싱 배너에서 [재인덱싱] 을 실행해 반영합니다.

### 신청이 막힌다

이미 처리 대기 중인 승인 건이 있는 드라이브에서 새 편집을 신청하면 "처리 대기 중인 승인 건이 있습니다…" 안내가 표시됩니다. 앞선 승인 건이 결재(승인·반려)된 뒤 다시 신청합니다.

---

# 임시 코드 검수 메모: 리스크 및 보완 필요 사항

> 아직 열려 있는(BE 미구현·후속) 항목입니다.

- **stub 잔존 1종**: 용어명 중복 검사(`checkTermDuplicate`)만 BE 엔드포인트가 없어 프론트 stub으로 동작합니다. 용어명 중복은 실제로는 추가·수정 시 서버가 `00030005`로 응답해 검출됩니다. (변경 신청 취소는 #15259에서 실 API `cancelTermApproval`로 전환되어 stub이 제거되었습니다.)

## Changelog

- v1.8.9 (2026-07-08): 최초 작성. 코드·기술문서(FE_TERMINOLOGY.md) 기반. #14444 외 관련 이슈 세트.
- v1.8.9 (2026-07-08): 실화면 스크린샷 11종(01~06·08~12) 적용 + 캡처와 대조해 토글 라벨([파일 | 용어사전])·추가 모달 적용 속성·운영 이력 컬럼 구성 교정. 07(영향 범위) 미확보로 "영향 범위 보기" 섹션은 주석 처리(숨김) 유지. #15106
- v1.8.9 (2026-07-09): 코드 drift 정정. 변경 신청 취소 stub이 실 API `cancelTermApproval`로 전환되어 stub 잔존 2종→1종(용어명 중복 검사) 정정(#15259). 영구 보관 200개 초과 오류 문구를 BE 응답 메시지 처리로 완화(#15496, FE 고정 문구 제거). 200개 제한 수치는 유지.
- v1.8.9 (2026-07-09): 문서 QA 정합 보정. API 표에 신규 용어 영향도 분석(`GET /impact`)이 현재 임시 비활성(#15315)임을 명시하고, 대기 승인 신청 취소 엔드포인트(`POST /approvals/{approval_id}/cancel`)를 추가. 신규 용어 추가 시나리오에서 현재 비노출인 "영향 범위 확인" 단계를 제거.